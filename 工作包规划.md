# Multiverse v1.0 稳定可大范围试用版：工作包规划

规划版本：2.0.0；日期：2026-09-26。交付目标是 **Agent 优先开发、可移植的人机协作框架 v1.0.0**。本篇是后续开发 Agent 的单一施工入口，包含全部工作包、执行/测试/独立审阅/推送规则、验收追踪与交接模板。

本次任务只完成规划和已有改动的审阅推送，不提前实施下列32个里程碑。所有实现状态初始为 pending，历史测试不能把未来工作标成完成。

## 0. 交给下一个 Agent 的启动指令

> 在 `/home/aied/tanyicheng/multiverse` 阅读 AGENTS.md、本篇《工作包规划.md》和 `docs/development/STATE.json`。保留已有修改，先处理待推送回执、到期的waiting_external检查或需要前台续跑的活动工作包；未到期的隔离后台验证按§3.1保留后可领取其他就绪包，否则按STATE顺序选择依赖已完成且已推送的首个就绪包。严格按包内步骤实现，跑指定测试与共同门禁，实际调用独立子Agent审阅；修复、复测、让reviewer复审最终快照。验收与审阅通过后提交并推送 `origin/dev`，核对远端提交，更新状态与交接证据。用户已授权每个里程碑完成后推送，不重复询问。缺外部条件如实blocked，继续依赖已满足的独立工作，禁止mock/skip/模拟review冒充完成。默认一次执行一个工作包；获得连续执行指令时逐包完成相同闭环。最终目标是W29定义的v1.0稳定试用交付，不是仅完成W13预览。

## 1. 权威、基线和范围

- 产品行为的唯一规范为 [MULTIVERSE_SPEC.md](docs/spec/MULTIVERSE_SPEC.md)，当前修订2.1.0；本篇分解施工和验收。新的v1要求在W14同轮写入该规范后才成为公开契约，不建立第二套线级协议。
- 本篇替代上一轮16份独立工作包的执行地位；旧分包仅作迁移导航，不再另行维护内容。`STATE.json` 只保存状态、依赖、证据，不定义另一套工作范围。
- 代码基线HEAD为 `b0d6c1b`。规划前已有本次会话未提交的研究、规范、作者指南、preflight及14个新测试；曾实际跑通209项测试、Ruff和mypy。W00核验当前真实版本，不能硬编码该数字或重新覆盖这些文件。
- 核心优势：Agent通过文件/Schema/能力发现/CLI编写；业务包与Binding分离；人、程序和Agent共享契约；事实/审批/产物可恢复；同一包跨CLI、机器和宿主运行。
- v1稳定的是公开合约和已声明支持组合；不承诺所有平台/插件无条件可接、不承诺原生session跨CLI无损迁移、不承诺所有外部副作用exactly-once。
- 暂不扩展敌对多租户共享宿主、多活图调度、完整组织管理SaaS、通用向量记忆平台、视觉编辑器全量功能或连接器市场。这些不应拖延第一条可用路径。
- 保留三个阶段出口：W04本地真实闭环；W13可移植预览；W29稳定试用候选。前两者可以提供试用，但不能改名充当完整稳定版。

## 2. 不可破坏的设计约束

1. **Agent是第一开发者。**文件/Schema/CLI/API足以完成主要操作；GUI辅助理解、审阅、人工处理与编辑；布局变化不改变业务语义。
2. **统一运行事实。**Ledger拥有业务状态，checkpoint只是控制进度，Connector/执行宿主管自己的执行关联；不再造一个能改写流程事实的聊天后台。
3. **身份与会话解耦。**AgentIdentity、ProfileRevision、协作thread、AgentSession、NativeSessionBinding、WorkspaceRevision、PolicyRevision和Invocation/Attempt分别建模。
4. **续接诚实。**跨CLI默认新建目标原生session，通过授权事实/Artifact/摘要续接；活动或unknown副作用先核对，不能换机器重做。
5. **权限实际实施。**授权、审批与沙箱分开；权限不足有分类、窄工具代理/环境修复/授权申请路径，不能自动关闭沙箱。旧审批不能跨输入/工作区/策略/身份变化继承。
6. **默认安全且可独立运行。**trusted-local明确选择，team使用真实身份/隔离；无飞书、无外部遥测、无latent依赖时标准路径仍可用。
7. **真实证据分层。**fixture、协议测试、真实模型、真人操作、跨机器、离线、恢复、性能证据分别保存，互不冒充。
8. **已有规范门槛不能被施工计划删除。**PostgreSQL、LangGraph、离线和独立真实latent实验交付均保留；任何范围变更须显式修订规范及对应验收，不能为了提前完成把失败项移出分母。

### 2.1 会话、记忆、协作和沙箱的明确决策

| 问题 | v1 决策与实施位置 |
|---|---|
| 一个 Agent 必须绑定一个会话吗？ | 不必须。身份可尚无会话或拥有多个隔离会话；一次 Invocation 明确选定会话/新建策略，禁止隐式选“最新”。W06 |
| 必须绑定某台机器某个 CLI 的原生 session 吗？ | 仅需原生连续上下文时建立 NativeSessionBinding；无状态任务无需原生持久会话。绑定含实例/存储/provider/版本，不能仅凭 session 字符串恢复。W03/W06 |
| 能切换 CLI、服务器或工作区吗？ | 能，受能力与授权约束。同后端允许经验证的 resume/fork；跨后端新 session + 授权 handoff。先核对在途副作用，工作区与策略修订变更重新授权。W07/W09 |
| 多 Agent 怎么协作？ | 用工作流任务、版本化 Artifact、授权事实及显式 handoff；同会话单写租约，默认独立可写工作区。合并冲突交给程序验证或人工决定，不共享无保护 transcript。W06/W07/W16 |
| 记忆属于谁？ | 业务事实归 Ledger；运行上下文归 session；可复用记忆属于带 namespace/主体/来源/版本/可见范围的显式记录，不能从原生 transcript 自动获得跨身份可见性。W07/W18/W21 |
| v1 的记忆做到什么程度？ | 实现授权上下文记录的写入/读取/撤销/删除和 handoff，不强制向量检索。自动语义记忆默认关闭，provider 扩展不成为业务真相；策略收窄重新构造干净上下文，缓存受撤销约束。W07/W18 |
| 何时使用沙箱？ | 不可信包/代码、多人服务和受限任务要求实际隔离；个人明确可信项目可显式 trusted-local；只读远程 API 工具采用受限身份和代理，不强求容器套容器。按声明的风险/能力选择并留下证据。W08/W18 |
| 沙箱权限不足怎么办？ | 先区分配置缺失与授权不足，优先修复依赖/最小挂载或窄工具代理；确需扩权则有权主体批准特定动作/资源，新建策略修订并重新预检。不能自动重试成全权限或复制旧审批。W07/W08/W18 |

参考竞品的详细比较、官方来源和仓库快照见 [研究报告](docs/research/2026-09-26-portability-and-agent-authoring.md)。实施 Agent 应复核所采用版本的官方接口；高星仅是调查线索，不替代许可证、兼容性和真实测试判断。

## 3. 每个里程碑的固定执行流程

### 3.1 开始、续跑和选择任务

```bash
pwd
realpath .
git status --short --branch
git remote -v
```

核实项目路径和 `origin = git@github-regrevia:regrevia/multiverse.git`。开发分支 `dev`，用户已有分支/改动不得覆盖；需要整合并发变更时用独立worktree保护。

先读取状态：有待推送提交/回执先续推；有 `in_progress/testing/reviewing/ready_to_push` 则恢复原工作包；有多个活动包先记录冲突，按已有owner/最早开始者协调，不重复领取。仅无需要前台续跑的活动包时选择STATE顺序中依赖全完成且已推送的pending。blocked条件解除后按已有checkpoint恢复，不从头重做。用户明确指定包仍须核验依赖。

**W14刻意排在W01后**，先冻结版本/支持范围和验收追踪，减少后期返工。W05A/B依赖W03而不等待W04真人现场验收，允许独立开发继续；W13显式汇合W04。飞书/真实宿主/离线资源/实验硬件阻塞不能禁止其他就绪包继续。

长时外部验证允许进入 `waiting_external`，不是completed：必须记录固定源码commit、产物digest、独立worktree/测试环境、实际后台任务标识、日志与checkpoint、下次检查时间和恢复命令。每轮先检查到期验证；未到期且无需前台处理时可以领取其他就绪包，依赖waiting_external的包仍不可执行。后台任务不占用正在改动的工作树/数据库/端口；新实现不得偷偷改变正在测的候选。受影响修复生成新候选并按W27/W29重测，原测试保留历史证据。缺隔离或不可恢复信息时不得让它后台运行后继续改同一环境。

每包先保存HEAD、已修改/未跟踪文件及内容摘要，确认已有能力。代码路径简写如 `runtime/`、`execution_host/` 均相对 `src/multiverse_workflow/`；`tests/`、`schemas/`、`inspector/`、`docs/` 相对仓库根。标为“新增建议”的文件今天可以不存在，实施时创建并纳入测试；若因现有结构改名，在报告中映射，不重新发明范围。

### 3.2 实现

先读指定源码/规范，再按步骤做小增量；公共接口同轮冻结Schema、类型、错误、权限、幂等、版本及正反例。外部CLI/API以安装版本及官方文档核对，不猜测当前字段。缺真实凭据/预算/真人/第二机器，只完成不依赖条件的工作并准确blocked，不扫描私人会话、不发送未授权真实消息。

同一包可跨多轮Agent会话保存checkpoint，但只有完整验收后算里程碑完成。不得把每次上下文中断当成里程碑完成并跳过独立审阅。

### 3.3 必跑测试

Python实现包在仓库根目录执行：

```bash
uv sync --locked
uv run --locked pytest -q
uv run --locked ruff check src tests
uv run --locked mypy src
git diff --check
```

uv不在PATH时先核实已有安装或在隔离工具环境安装；W00必须形成持久可重复路径，不依赖临时目录。依赖需新增时显式更新锁文件与变更理由，然后用 `--locked` 验证新锁；不能删除锁文件求成功。

前端/HTTP客户端或影响前端API的包，在 `inspector/` 额外执行：

```bash
npm ci
npm run test:run
npm run build
```

选择满足锁定engines的Node版本；不能假定当前Node20能支持后续依赖。新SDK/容器/存储/模型测试必须加入本包指定检查和CI，不因上述基础命令没覆盖就免测。

开发中先跑定向测试；收尾跑适用完整门禁。纯文档变更检查链接、JSON、依赖无环、状态和规范一致性，不无故运行付费模型。测试记录cwd、命令、工具版本、时间、退出码、passed/failed/skipped、日志索引。预期失败负例显式断言退出码；禁止 `|| true` 抹掉失败。live的skip不算通过。需要真人的实际决定不能由作者Agent代批。

### 3.4 必须实际调用独立子 Agent

固定顺序：**实现 → 测试通过 → 独立子Agent审阅 → 修复 → 复测 → 子Agent复审最终快照 → 提交推送。**

每个包首次review创建独立reviewer，不能让实现子Agent审自己的改动。本环境调用 `collaboration.spawn_agent`，其他环境使用等效独立子Agent工具；不存在工具则reviewing/blocked，不能自拟审阅结果。review期间主Agent只读检查/整理证据，不修改reviewer正在看的快照。

调用提示须包含工作包ID、规范章节、HEAD及原有改动清单、**包含未跟踪文件**的本轮文件SHA256、测试/真实验收报告。要求reviewer直接读实际代码/新文件，检查权限、幂等、崩溃窗口、unknown、取消竞争、产物、会话/工作区、兼容和Agent可修复诊断，返回文件:行和严重级别。

- P0/P1必须关闭；影响验收/数据/权限/恢复/契约的P2也必须关闭。
- 非阻塞P2/P3只有reviewer明确同意才可记录后续owner/工作包；主Agent不得单方忽略。
- 修复后复测并用 `followup_task` 或等效方式请求原reviewer复审新摘要；旧approved不能沿用到新代码/Schema/验收内容。
- `approved`仍需实际审阅范围和限制；单元测试绿不替代真实验收。
- 纯日志/推送回执补录可追加不触发代码重审，但不得更改原证据或结论。

### 3.5 提交、推送、核对远端（用户已授权）

本次用户授权：**当前规划处理完成后，以及以后每个里程碑完成后，都提交并推送到origin/dev。**不重复请求许可。此授权不包括force push、改写他人历史、生产部署、自动创建正式Release或发布软件注册表。

1. 测试与review通过后准确暂存本轮文件/改动和报告，不使用 `git add .` 混入私人文件；前序本会话成果需要随本次推送时明确列清并保留。
2. 生成实现提交C，例如 `feat: complete W03 codex bridge`；`git fetch origin dev`，确认远端没有未整合改动，使用正常 `git push origin HEAD:dev`。
3. 用 `git ls-remote origin refs/heads/dev` 和 `git rev-parse HEAD` 核对。若远端已被后续提交推进，fetch后用 `git merge-base --is-ancestor <C> origin/dev` 证明C在远端历史中；不能只凭push输出一句success。
4. C确认已远端包含后，把STATE该包改为completed并填 `implementation_commit=C`、报告路径；创建单独状态回执提交R，推送R并再次核对。这样状态不需要引用其自身提交ID，也不会出现自引用摘要循环。
5. 只有实现C和状态回执R均到达远端才开始依赖包。R推送失败时，本地已通过的实现不重做；将“回执待推送”和R记录到受控本地日志，下一轮优先重试同一个R。代码推送失败时维持ready_to_push/push_pending，不能用completed掩盖失败。
6. 非快进不得force；读取并整合远端变更，保护用户未提交内容。整合改变实现快照就重跑受影响测试和review，再正常推送。网络/权限失败报告真实原因与待推commit，不伪称同步。
7. 开始依赖包时验证STATE完成回执已存在远端，以及 `implementation_commit` 为远端祖先；仅本地修改JSON不能越过门禁。

提交与推送不自动终止已授权的连续执行；每包完成同样流程再进入下一包。长时soak/真实试用等待通过记录续跑，不能空等阻塞其他独立工作。

### 3.6 状态与报告

运行状态为 `pending/in_progress/testing/waiting_external/reviewing/blocked/ready_to_push/push_pending/completed`。`implementation_ready=true` 仅说明代码准备好，不代表真实验收完成。仅将外部条件记录到 `blockers`；不把等待审阅批准视作用户还未授权。

每轮报告放 `docs/development/reports/<ID>/`；报告包含范围、基线、本轮快照、验收矩阵、完整测试索引、真实reviewer task ID/结论、修复复测、限制、实现commit和推送证据。敏感日志、数据库、原生transcript、private eval和Secret仅保留受控本地引用，不进入Git。

W00实现轻量 `scripts/milestones.py`（此规划时尚不存在）及其测试，提供 `next`、`check-plan`、`check <ID>`：校验状态/依赖/验收报告/测试结果/reviewer真实记录引用/最终摘要与远端祖先关系，拒绝缺项或陈旧证据。它验证记录一致性，不宣称能凭JSON自行证明真人或子Agent行动；行动仍由实际工具回执佐证。

## 4. 路线、优先级和快速交付

“快”通过先交付小闭环、复用已有模块、去掉不必要外部依赖实现，不承诺未经测试的天数。不要在W03前建设完整插件市场、全框架重构或团队SaaS。

| 出口 | 必须交付 | 对外称谓 |
|---|---|---|
| W04 | 真实Codex→程序验证→真人→产物，重启可恢复 | 本地可信开发预览 |
| W13 | 多CLI、远程、飞书、宿主、同包换Binding和干净在线交付 | 可移植协作预览 |
| W24—W28 | 受控团队服务、SDK、离线、安全、安装升级、稳定性和真实试用 | Beta/RC，准确列未验证项 |
| W29 | 现行全部规范门槛+稳定性/试用门槛+独立review+固定产物 | v1.0.0稳定可大范围试用，发布操作另记 |

所有包都必须独立测试、review和push；编号不直接决定先后，以下顺序及显式依赖决定。


| 包 | 交付 | 依赖 | 规范阶段 |
|---|---|---|---|
| [W00](#w00) | 保全基线与建立可重复验证 | 无 | M0/M1 已有能力核验 |
| [W01](#w01) | Agent 可发现配置与可注入注册表 | W00 | M0；G-01/G-02/G-03/G-19 最小子集 |
| [W14](#w14) | v1 发布契约、需求追踪与支持范围冻结 | W00, W01 | M0/M7；全 G 项和验收集建账 |
| [W02](#w02) | 最小执行宿主与持久 HTTP Job Bridge | W01 | M2/M3；G-04/G-08/G-12 子集 |
| [W03](#w03) | 首个真实 Coding Agent：Codex | W02 | M3；G-03/G-04/G-08/G-11 最小闭环 |
| [W04](#w04) | 交付首条真实人机协作闭环 | W03 | M4；G-09 最小人工入口 |
| [W05A](#w05a) | 第二种执行端：Claude | W03 | M3/M5 适配复用验证 |
| [W05B](#w05b) | 第三种执行端：Pi | W03 | M3/M5 适配复用验证 |
| [W06](#w06) | 身份、多会话与原生恢复 | W05A | M3/M5；G-17 第一子集 |
| [W07](#w07) | 跨 CLI 续接、工作区与授权上下文 | W06 | M3/M5；G-17/G-18 切换子集 |
| [W08](#w08) | 一个真实隔离后端与可信本地模式 | W07 | M3；G-05/G-18 有限平台实现 |
| [W09](#w09) | 远程电脑出站 Connector | W08 | M5；G-06/G-07/G-12 |
| [W10A](#w10a) | 飞书人工任务通道 | W04 | M5；G-10 人工通道 |
| [W10B](#w10b) | 飞书程序节点与副作用核对 | W10A, W02 | M5 外部程序执行 |
| [W11](#w11) | 一个真实宿主与独立 Console 共用接口 | W04 | M6；G-15 子集 |
| [W12](#w12) | 干净目标交付与最小安装闭环 | W09, W11 | M6；G-13/G-14/G-16 声明子集 |
| [W13](#w13) | 作者效率、故障恢复与交付总验收 | W04, W05B, W07, W08, W09, W10B, W11, W12 | M7 可移植预览验收，不替代完整发布 |
| [W15](#w15) | PostgreSQL 持久化、迁移与单活服务模式 | W14, W02 | M1/M2/M6；规范既定持久层 |
| [W16](#w16) | LangGraph 后端、一致性与完整组合语义 | W15, W07 | M1/M2/M4；全部控制流与 checkpoint |
| [W17](#w17) | 团队身份、授权与所有入口一致性 | W14, W11, W09 | M2/M6；可信主体与 namespace隔离 |
| [W18](#w18) | Secret Provider、受控提权与审计闭环 | W17, W08, W07 | M2/M3；G-05/G-18闭合 |
| [W19](#w19) | 完整 Agent 作者工具链：创建、修复、评测、Diff | W14, W13, W16 | M4/M7；G-19闭合 |
| [W20](#w20) | 可嵌入 SDK、完整人工入口与可访问 Console | W17, W19, W04 | M4/M6；G-09/G-15闭合 |
| [W21](#w21) | 运维、备份恢复、数据保留与故障手册 | W15, W16, W18 | M6/M7 服务可运维性 |
| [W22](#w22) | 完全本地真实 Agent 与离线交付 | W12, W18 | M3/M6；G-16及离线强制验收 |
| [W23](#w23) | 独立可安装的真实 Latent 实验实现 | W14, W16, W18 | E1/E2；LH-01—LH-14 |
| [W24](#w24) | 发行包、供应链与升级回滚演练 | W12, W15, W19, W20 | M6/M7；G-13/G-14 完整发行形态 |
| [W25](#w25) | 支持矩阵、协议兼容与跨平台合约 | W24, W05B, W09, W22 | M7 兼容性 |
| [W26](#w26) | 安全、滥用边界与数据隔离总审计 | W17, W18, W20, W24 | M7 安全发布门槛 |
| [W27](#w27) | 性能、连续运行与灾难恢复压测 | W16, W21, W25, W26 | M7 稳定性门槛 |
| [W28](#w28) | 候选版真实试用与反馈闭环 | W19, W20, W22, W24, W26 | M7 Beta/RC 外部可用性 |
| [W29](#w29) | v1.0 稳定试用版最终签收与发布准备 | W14, W15, W16, W17, W18, W19, W20, W21, W22, W23, W24, W25, W26, W27, W28 | M7 完整项目级门槛 |


## 5. 详细工作包


<a id="w00"></a>

### W00 · 保全基线与建立可重复验证

依赖：无；对应阶段：M0/M1 已有能力核验。

#### 结果与边界

后续 Agent 能拿到准确起点、可靠命令和未完成清单。不扩展业务功能，不把前序未提交改动丢掉；只补本篇要求的轻量里程碑检查器。

#### 文件

读取：`pyproject.toml`、`uv.lock`、`inspector/package*.json`、当前 git diff、`docs/spec/`、`docs/authoring/`。
更新：`docs/development/STATE.json`、新增 `reports/W00/REPORT.md`；必要时添加最小 CI 配置与工具版本说明，不更换现有依赖管理器。

#### 步骤

1. 记录 HEAD、分支、已修改/未跟踪文件、Python/uv/Node/npm 版本。前序 preflight 代码应存在，不再生成一次。
2. 检查现有工具；uv 可能在临时工具目录，本轮将可重复安装/调用方式写清，不能让后续任务依赖 `/tmp` 中偶然存在的二进制。使用隔离环境安装，不改系统 Python。
3. 以 `uv sync --locked` 恢复 Python 环境。核对前端锁定依赖的 Node engines，安装/选择兼容版本；保存版本要求，不凭当前 Node 20 能启动就视作兼容。
4. 跑完整 Python 门禁，以及 `npm ci`、`npm run test:run`、`npm run build`。分别记录环境问题和代码失败，不修改业务断言求绿。
5. 跑 capabilities、validate、preflight 的本地正例及远程负例；断言远程负例退出 2，且不创建执行。
6. 建立里程碑证据目录约定、快照方法和报告实例；记录已有功能，不重构 Runner/数据库。
7. 实现本篇§3.6的 scripts/milestones.py 与 tests/development/test_milestone_gate.py；覆盖恢复活动包、依赖未推送、缺review记录、旧快照、测试失败、live skip、推送失败/回执待推送不得推进；覆盖waiting_external到期恢复、隔离信息缺失拒绝、可领取无依赖就绪包及不能解锁依赖包。实际工具记录仍是验收依据。

#### 验收

- 当前全量测试实际结果可查；209 是历史数字，不硬编码新测试总数。
- Python/前端可以从锁文件重复安装，完整命令在报告可复制。
- 未提交成果均保留；snapshot 覆盖未跟踪源码，不包含凭据/真实数据库。
- 独立 reviewer 核对基线和前序改动边界，确认后续无需重新决定环境。


**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w01"></a>

### W01 · Agent 可发现配置与可注入注册表

依赖：W00；对应阶段：M0；G-01/G-02/G-03/G-19 最小子集。

#### 结果与边界

作者 Agent 能找到真实执行器及配置 Schema，填写 Binding 后在执行前发现配置错误。自定义受信任注册表能穿过 CLI/服务/Worker；当前 RuntimeApplication 内部构造 Runner，必须处理这条注入路径。先做显式注册，不做插件市场、自动下载或动态任意 Python import。

#### 文件

修改 `runtime/registry.py`、`compiler/preflight.py`、`cli/main.py`、`service/application.py`、`api/dependencies.py`、`runtime/worker.py` 的必要入口。
新增建议：`runtime/catalog.py`、`schemas/executor-registration.schema.json`、`tests/runtime/test_catalog.py`、`tests/cli/test_catalog.py`；扩展 `tests/service/`、`tests/api/`。

#### 步骤

1. 在规范冻结本地注册配置：executorRef、版本、adapter、能力、配置 Schema 引用/摘要、取消/恢复/权限保证；必须区别声明和验证证据。
2. 用 JSON Schema 描述 builtin/local_process/http_job/human 的已支持配置；拒绝未知必需配置，不允许 config 任意透传绕过后端白名单。
3. 复用现有 `capabilities --json`，增加按执行器查询 Schema 的能力；具体命令同轮固定并写入 CLI help/指南。
4. 保留默认注册表，同时允许应用构造器、CLI serve/run/worker 使用一个显式受信任目录；只允许已实现 adapter。安装/验证由管理路径控制，包本身不能自称 verified。
5. preflight 输出精确 config 字段路径、expected/actual；只有已实现配置校验后才从 notChecked 中移除对应项目。记录“验证配置形状”与“探测真实服务”的区别。
6. 补兼容测试：旧本地 Binding 仍可用；HTTP Job 测试注册表通过所有入口后可以运行；service/worker 不能出现不同目录快照。

#### 测试与验收

定向：`uv run --locked pytest tests/runtime/test_catalog.py tests/cli/test_catalog.py tests/cli/test_preflight.py tests/service tests/api -q`。
非法命令数组、超时范围、未知字段/执行器、路径越界、伪造 verified、目录撤销均有负例。输出不泄露配置内 Secret；已有目录调用不破坏。
作者只依赖 help、目录和 Schema 即可修复一个损坏 Binding；不必编辑 Python 注册常量。尚未实现的插件安装 ABI 不宣称完成。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w14"></a>

### W14 · v1 发布契约、需求追踪与支持范围冻结

依赖：W00, W01；对应阶段：M0/M7；全 G 项和验收集建账。

#### 结果与边界

把开发预览升级为可执行的稳定版范围：软件版本 v1.0.0 不自动改变当前线级 multiverse/v0.1。先盘点兼容策略，再在规范中冻结公开线级版本；不得只改版本号冒充稳定。此包可在 W01 后提前执行，与早期真实接入并行规划；优先序见状态表。

#### 文件与步骤

新增 `docs/release/SUPPORT_MATRIX.md`、`docs/release/ACCEPTANCE.json`、版本兼容测试与部署 profile Schema；更新唯一规范，不新增并行产品规范。
1. 从规范提取 AC/LH/SUP/RDI/CMP/PORT 和 G 全部 ID，生成逐条记录：主负责工作包、必需证据层、测试路径/报告、pending/failed/passed/blocked。禁止只有范围表却无逐条追踪。
2. 对现有代码做能力盘点，逐项标 implemented/verified/unsupported；现有图调度、并行汇合、默认权限保证不能仅靠注释声明。
3. 固定三种发行 profile：personal（SQLite 单用户）；team（PostgreSQL 单活调度、同组织多主体/namespace）；offline（真实本地模型/程序/人工）。远程 Connector、飞书、CLI后端为按需安装能力；“可选安装”不免除项目级真实验收。
4. 默认 v1 支持基线：Linux x86_64 服务与沙箱；Linux x86_64/macOS arm64 的受支持本地 Connector；Python 3.12/3.13；浏览器与 Node 版本按锁文件/实机矩阵冻结。Windows 原生、敌对租户共享宿主、多活图调度不进入 v1承诺；可以记录未支持，不能宣传任意环境无条件即插即用。
5. 固定包/Binding/HTTP Job/HTTP API/CLI JSON/事件/SDK 的版本策略、未知字段行为、弃用期限与 v0.1 导入路径。原生 CLI版本明确到实测区间，不跟随 latest 自动更新。
6. 将本计划新增稳定性/SLO/试用门槛按设计要求合入唯一规范，冲突先解开；不得暗中删掉已有 PostgreSQL、LangGraph、离线和真实 latent 验收。

#### 验收

逐 ID 追踪无漏项、无重复 ID、每项有负责人和证据类型；范围矩阵与 README/CLI支持声明一致；不把未实测平台写为 supported。
新增静态契约测试纳入 pytest；未知版本、旧包导入、过期客户端行为有可执行测试设计。该包本身冻结要求，不伪造后续通过证据。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w02"></a>

### W02 · 最小执行宿主与持久 HTTP Job Bridge

依赖：W01；对应阶段：M2/M3；G-04/G-08/G-12 子集。

#### 结果与边界

建立一个可复用的标准 HTTP Job 服务端，用确定性子进程验证持久执行、观察、取消和产物链路。它不是第二个 Workflow Runtime；拥有本地执行事实和原生关联，不做图路由。本轮仅 loopback 可信开发模式，禁止当作已认证公网服务。

#### 文件

复用 `runtime/http_job.py`、`runtime/runner.py`、`runtime/ledger.py` 与既有 HTTP Job 测试。
新增建议：`execution_host/store.py`、`execution_host/service.py`、`execution_host/backend.py`、`tests/execution_host/test_store.py`、`test_http_job_service.py`、`test_crash_recovery.py`。

#### 步骤

1. 冻结后端最小接口：启动、查询、取消、事件、产物，以及支持声明。协议以规范 §9 既有 HTTP Job 为外层，不增加另一套生命周期。
2. 复用现有客户端端点，实现 descriptor、submit、dispatch-key lookup、observe、cancel、artifact metadata；以客户端契约测试反向验证服务端。
3. 宿主用独立 SQLite 文件记录 dispatchKey、请求摘要、executionRef、状态修订、事件游标及实际子进程/会话关联。同 key 同摘要复用；不同摘要冲突。
4. 覆盖 intent 落库 → 原生启动 → 原生 ID 落库之间的崩溃窗口：无法证明未启动时 unknown，禁止自动再次启动。不把 PID 存在或进程缺失当作外部副作用结论。
5. 观察状态单调且重复 revision 内容一致；取消仅操作任务拥有的子进程树，有明确 unsupported/best effort/confirmed，不杀共享服务。
6. 明确产物字节落点：本机模式经 Runtime 受控登记路径形成真实 ArtifactRef，不把宿主路径/URL当 Artifact。为后续远程准备带摘要/大小/命名空间的受控传输接口；本轮至少完成本机完整产物链。
7. JSON 结果、日志、流式事件分离；限制输出大小/事件积压，不回显环境凭据；提供启动文档和最小测试 Binding。

#### 测试与验收

定向：`uv run --locked pytest tests/execution_host tests/runtime/test_http_job.py tests/runtime/test_http_job_runtime.py -q`。
通过真实本地子进程证明：重复提交只启动一次；断开 submit 响应后查回同 executionRef；重启服务后仍可查询；启动窗口不明进入 unknown；重复/乱序观察被处理；超大/非法输出失败有证据。
程序节点的真实文件通过 Runtime 校验/登记后可读取。直接拷贝业务库或绕过 Artifact ACL 不算完成。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w03"></a>

### W03 · 首个真实 Coding Agent：Codex

依赖：W02；对应阶段：M3；G-03/G-04/G-08/G-11 最小闭环。

#### 结果与边界

使用机器已有 Codex，在专门测试工作区完成真实任务，走标准 Bridge→Runtime→验证→等待人工。只做新会话与一个固定任务模板；多会话恢复交给 W06。trusted-local 支持范围必须准确，未具备沙箱不能称隔离。

#### 文件

新增建议：`execution_host/backends/codex.py`、`tests/execution_host/test_codex.py`、`tests/integration/test_codex_live.py`、`examples/bindings/content-codex.yaml`。
复用宿主、既有内容交付包、程序 verifier、Artifact 登记；配置/命令加入 W01 目录。

#### 步骤

1. 探测 Codex 安装版本和官方 App Server 文档；按安装版本导出/锁定协议，保存兼容矩阵与源码链接。不遍历个人历史或打印 auth 文件。
2. 显式指定测试工作区、配置版本和新会话；用结构化连接初始化、请求 ID 关联、事件消费驱动后端，不能解析终端展示文本。
3. 提交确认不当完成；记录原生 thread/turn 与 Attempt/executionRef 的关联。传输丢失按 W02 unknown 规则处理。
4. 设置可实施的超时、输出/费用或轮次界限（无法精确限制的项报告 unsupported）；使用现有授权测试身份，不改全局 CLI 配置。
5. 同轮冻结 G-11 最小原生交互子集：持久 interactionId、execution/native request 关联、请求 kind、输入 Schema、动作/资源/主题摘要、授权主体、期限、版本与 pending/replied/expired/invalid 状态。新增受控回复 CLI/API 和 Bridge→原生请求响应接口；它不同于业务 HumanRequest，也不是任意聊天追加接口。只有本次实际请求的授权主体能回复；回执幂等键+正文摘要+expectedVersion 固定。拒绝/过期不自动批准。
6. 回复先持久化意图，再投递；重复同回复返回原回执，正文不同冲突。响应丢失不能无条件重发；原生后端不支持查询/幂等时进入待核对。原生连接重启无法证明原请求仍有效时标 invalid/unknown，拒绝旧批准，不重新触发动作。工具许可与业务审阅分别记录。最小交互用 CLI/API 操作，GUI 不作为 W03 前置。
7. 固定模型输出为业务 Schema；通过真实文件/内容登记 Artifact，程序验收后 Run 进入 HumanRequest。记录版本、Run 和产物摘要，不保存私有转录到仓库。

#### 测试与验收

新增协议 fixture 覆盖握手失败、请求/事件交错、完成事件、拒绝/取消、超时、输出截断、断线未知；完整 Python 门禁必须通过。
真实命令形式为 `uv run --locked pytest tests/integration/test_codex_live.py -q`，测试在本包实现；缺授权/后端时 fixture 可通过，但该包保持 blocked，live skip 不算完成。
至少一次真实任务产生可验证产物，经过 Runtime 程序节点到人工待办；重放网络请求不再次发模型任务。不得把 echo/固定内容作为 live 证据。

原生交互验收必须覆盖：实际可触发的一次原生输入或许可请求、有效回复、拒绝、过期、未授权主体、重复/冲突回复、成功响应丢失、原生连接失效。所有失败路径用协议 fixture 验证；至少一条请求→回复→继续的真实原生链路，不能由观察到 waiting 代替。若所选安装版本完全没有可用交互能力，不能标 G-11 子集完成；固定版本/范围后另行提供真实能力。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w04"></a>

### W04 · 交付首条真实人机协作闭环

依赖：W03；对应阶段：M4；G-09 最小人工入口。

#### 结果与边界

让人能从现有 Inspector/独立 Console 看到真实待办、证据、提交决定并完成流程。已有 client、human 表单和控制若已满足则复用，只补缺口。不新建图形编辑器。

#### 文件

读取/必要修改 `inspector/src/runtime/client.ts`、`human.ts`、`App.tsx`、`api/app.py`、`service/application.py`。
扩展 `inspector/src/runtime/*.test.ts`、`tests/e2e/test_service_workflow.py`；新增建议 `tests/e2e/test_coding_delivery.py` 与脱敏演示步骤。

#### 步骤

1. 实测现有人类请求查询、表单、产物读取、决定提交，列差距后仅补差距。
2. 待办显示主题、输入/产物证据、允许输出、当前版本、授权状态和失败原因；UI 关闭/重开仍从 Runtime 重建。
3. 提交使用真实主体、subjectDigest、expectedVersion、Idempotency-Key；重复点击去重、旧卡片/过期版本明确失败。
4. 跑完整真实 Codex → 程序验证 → 真人 approve/reject → Run 终态。自动测试可以用测试主体，实际演示必须由获授权的人做决定。
5. 停 Worker、重启、重新打开页面，验证待办/决定不丢且只推进一次；暂停时可记决定但不继续派发。
6. 更新最短可运行指南：启动服务、Worker、Bridge、打开待办、查证据。明确这是本地可信预览；记录第一条可交付成果。

#### 测试与验收

跑 e2e/服务/权限测试，以及前端 `npm run test:run`、`npm run build` 与完整 Python 门禁。
验收覆盖 approve/reject、重复提交、并发版本、未授权主体、过期请求、关闭页面、Worker 重启、暂停与取消。真实真人动作尚未发生则保持 blocked，可继续独立测试，不由作者代批。
到此必须有一份可复现的本地真实演示；后续功能不能拖延这一交付。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w05a"></a>

### W05A · 第二种执行端：Claude

依赖：W03；对应阶段：M3/M5 适配复用验证。

#### 结果与边界

接入已安装 Claude，通过换 Binding 运行同一业务包，检验宿主接口是否真的不依赖 Codex。本轮仅新会话，复杂续接留 W06/W07。

#### 文件与步骤

新增 `execution_host/backends/claude.py`、`tests/execution_host/test_claude.py`、`tests/integration/test_claude_live.py`、对应示例 Binding。
1. 查安装版本/官方 SDK 或结构化非交互接口，选择一种维护接口并锁版本；不直接读改原生 session 私有格式。
2. 映射配置、提交、事件、结束、错误、产物、输入请求和取消到 W02 接口，逐项报告保证不足。
3. 多用户/任务禁止“最近会话”；始终用新会话并记录实际原生关联。
4. 复用 W03 相同业务输入与验证；业务包 digest 保持不变，只换 Binding。
5. 发现接口缺口只做后端通用扩展，同轮让 Codex 合约回归通过；不在 Runner 写 provider 分支。

#### 验收与测试

fixture 覆盖事件分片、取消、请求丢失、非 JSON 输出和拒绝；跑 `tests/execution_host` 全部回归与 Python 门禁。
真实 Claude 任务通过既有程序验证与人工待办，记录包摘要与两个 Binding 摘要。缺原生登录/配额时 blocked，不私自换服务商 API 冒充同一运行时。


W04 真人现场验收不是本包的技术前置：本包以真实执行至程序验证/人工待办为止，完整真人闭环在 W04 和 W13 汇合。缺真人时不能阻塞本包。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w05b"></a>

### W05B · 第三种执行端：Pi

依赖：W03；对应阶段：M3/M5 适配复用验证。

#### 结果与边界

提供 Pi 的同构实现，验证长期 RPC 进程和其他 CLI 一致使用标准任务契约。当前未发现 Pi；缺安装可先完成 fixture，但 live 验收前必须安装固定版本并有获准模型配置。

#### 文件与步骤

新增 `execution_host/backends/pi.py`、`tests/execution_host/test_pi.py`、`tests/integration/test_pi_live.py`、示例 Binding。
1. 核对当前官方仓库/发行版本和 RPC 文档，锁版本及安装方法；不依赖会变的 main 分支事件名称。
2. 管理子进程 JSONL、请求 ID、异步事件、stderr、UTF-8/分片、输出背压；根据该版本契约判断“接受”和“完成”。
3. 一次任务显式新 session；只记录原生允许公开的关联，不共用全局会话目录。
4. 子进程退出、事件丢失/重复和取消不明复用 W02 的恢复语义；记录无法恢复的事实。
5. 跑同一业务包和输入，不改 Workflow，不在 Compiler/Runner 增加 Pi 专用分支。

#### 验收与测试

运行 `tests/execution_host`、`tests/integration/test_pi_live.py` 与完整 Python 门禁；缺 live 条件不能用 skip 完成。
提交确认不能提前完成 Attempt；真实 Pi 产物通过程序验证。与 W05A 无强制先后，可因 Pi 条件缺失继续其他已就绪工作。


W04 真人现场验收不是本包的技术前置：本包以真实执行至程序验证/人工待办为止，完整真人闭环在 W04 和 W13 汇合。缺真人时不能阻塞本包。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w06"></a>

### W06 · 身份、多会话与原生恢复

依赖：W05A；对应阶段：M3/M5；G-17 第一子集。

#### 结果与边界

同一 Agent 拥有多个隔离 session，并可在支持的后端显式 resume/fork。只支持已验证版本/同原生存储，不在本包实现跨 CLI 迁移。W05B 未完成也可用 Codex+Claude 验证；Pi 之后走同一合约。

#### 文件

新增建议 `execution_host/sessions.py`、`service/session_contracts.py`、`schemas/agent-profile.schema.json`、`schemas/session-binding.schema.json`、`tests/execution_host/test_sessions.py`、`tests/service/test_sessions.py`；迁移现有 SQLite 需可向前升级。

#### 步骤

1. 冻结 AgentIdentity、不可变 ProfileRevision、协作 thread、AgentSession、NativeSessionBinding 的最小类型/错误/授权，不把这些塞进 Workflow 顶层 kind。
2. 记录 namespace、主体/scope、provider、安装/存储身份、原生 ID、版本、workspace/policy 修订和创建来源；原生 ID 的唯一性按完整复合身份约束。
3. 实现创建/查询、显式 resume、支持时 fork；不支持时返回明确错误，不退回最新会话。
4. 引入会话单写租约、版本比较和 fencing；同一会话并发 turn 只许一个写者，分叉生成新会话，工作区隔离独立处理。
5. 活动/unknown Attempt 在恢复前核对；租约失效不自动判旧进程死亡。关联变更与审计原子提交。
6. 最小 CLI/API 管理入口可看到原生关联、能力和恢复限制；列表按授权过滤；现有运行保持原 ProfileRevision。

#### 测试与验收

新增单元/服务/迁移测试；覆盖同 agent 多 session、同原生 ID 不同安装、跨主体读拒绝、失效租约、并发恢复、迟到事件、配置更新不污染旧 Run。
真实 Codex/Claude 各完成一次可支持的 resume；fork 只对该版本明确支持的后端声明并实测，不支持必须被准确显示。状态/上下文恢复与文件快照分开验收。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w07"></a>

### W07 · 跨 CLI 续接、工作区与授权上下文

依赖：W06；对应阶段：M3/M5；G-17/G-18 切换子集。

#### 结果与边界

同一逻辑 Agent/任务从 Codex 续接到 Claude（或已验证 Pi），通过受控材料继续。保持语义记忆默认关闭；先做显式、版本化的上下文记录，不引入向量数据库。

#### 文件与步骤

新增建议 `execution_host/handoff.py`、`execution_host/workspaces.py`、`execution_host/policy.py`、`schemas/handoff-envelope.schema.json`；测试 `tests/execution_host/test_handoff.py`、`test_workspaces.py`、`test_policy.py`。
1. 固定 handoff 内容：目标、约束、已确认事实/来源、ArtifactRefs、工作区基线、未决问题、下一步、上下文/策略版本及丢失项。排除旧凭据、审批和未决工具调用。
2. 材料逐项授权并校验摘要/版本；权限收窄不能把旧 transcript 原样注入。创建干净目标原生 session，保留来源链而不是覆盖旧关联。
3. 切换前停止新派发并核对在途工作；准备目标 → CAS 切换关联/fencing → 取得租约后执行；失败不丢旧关联。
4. 工作区用独立目录/快照或 Git worktree 实现第一种后端；文件交接记录内容摘要，路径相同不能当作内容相同；两个 Agent 默认独立可写副本。
5. 定义权限交集和不足分类；高权限工作拆程序/工具代理，权限请求持久化。批准必须由实际有权主体绑定动作/资源/摘要，不自动关闭沙箱。
6. Context/显式记忆记录实现最小写入、授权读取、撤销和删除；保存 namespace、主体、来源、可见范围、版本、注入片段摘要与裁剪信息。长期自动语义检索仅预留 provider 边界；禁止将摘要当业务完成事实，源被撤销后不得继续从缓存注入。

#### 测试与验收

故障测试：切换中断、目标创建失败、并发切换、旧 executor 迟到、源有 unknown、非法跨主体材料、工作区漂移、权限收窄、显式记忆撤销/删除后缓存再注入被拒。
真实示例：Codex 产出一个已登记文件，Claude 在独立目标上下文继续，程序验证最终结果；核对两段关联与来源。禁止靠搬运原生 session 文件实现“跨 CLI 无损恢复”。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w08"></a>

### W08 · 一个真实隔离后端与可信本地模式

依赖：W07；对应阶段：M3；G-05/G-18 有限平台实现。

#### 结果与边界

实现一个被验证的容器沙箱后端（优先当前目标可用的 Docker/OCI），保留明确 trusted-local 模式。先限定一个 Linux 配置，不承诺全 OS 或恶意多租户强隔离。

#### 文件与步骤

新增建议 `execution_host/sandbox.py`、`schemas/execution-policy.schema.json`、`tests/execution_host/test_sandbox_policy.py`、`tests/integration/test_sandbox_live.py`；扩展执行目标目录与 preflight。
1. 探测目标是否具备容器/用户命名空间、资源限制和网络控制；不具备时拒绝所需保证，不能静默回退本机执行。
2. 策略描述明确只读/可写挂载、网络、运行用户、资源/超时、进程终止。控制端 Docker socket、Runtime 数据库、宿主 HOME 和凭据不可挂进任务。
3. 实现工作区最小挂载、非特权身份、capabilities 收缩及实际可执行的网络规则；不支持的精细策略预检报错。外层已有隔离可复用，但记录实施者与证据。
4. 可信本地模式须明确选择和准确标识权限；不要因容器启动失败自动 trusted-local。
5. 错误分类显示缺依赖/挂载/网络/OS权限/工具 scope。新增授权冻结新策略并重新预检；不能用一句“批准”变成全宿主权限。
6. 验证取消只终止本次拥有的容器/子进程，不删除长期工作区或其他任务资源。

#### 测试与验收

真实容器负例：读取未挂载宿主文件、访问 Runtime DB、越界写、被拒网络、fork/超时资源限制、读取宿主凭据均被拒；同时获准任务可以成功。
容器不可用时标 blocked；mock subprocess 不足以证明隔离。生产多人强隔离另行验证，本包只声明实际测试的平台与保证。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w09"></a>

### W09 · 远程电脑出站 Connector

依赖：W08；对应阶段：M5；G-06/G-07/G-12。

#### 结果与边界

第二台电脑/服务器主动出站接入，平台能发现其获准 CLI/目录、派发、观察和撤销。保持一个图调度 Worker；Connector 只是执行端。

#### 文件与步骤

新增建议 `connector/`、`api/connector.py`、`service/connector_contracts.py`、`tests/connector/`、`tests/integration/test_connector_live.py`；扩展宿主/Artifact 服务。
1. 冻结短时单次配对、客户端身份、TLS 校验、令牌轮换/撤销、允许目录/执行配置。秘密只保留在受控凭据存储，不进包/日志。
2. 先做一种出站长轮询领取协议，复用已有 HTTP/事件模型；无必要不再加 WebSocket。领取/确认/回传具幂等身份、游标、大小和速率限制。
3. Connector 本地持久映射 dispatchKey→executionRef→native binding；重复领取不得再次启动。活动租约失效只标观察过期，不自动跨机器重派。
4. 实现断线事件缓存/有界积压、恢复 lookup、过期 fencing、显式停止领取/排空/撤销；单任务取消不等同关闭 Connector。
5. 完成远程 Artifact 字节上传/下载的受控闭环：认证、namespace/Run关联、大小、内容摘要、完成确认后登记。不能靠共享本机绝对路径或任意 URL 冒充远程产物。
6. 管理页/CLI区分在线、最后观察时间、任务状态、恢复保证与未授权资源；未配对/未选择目录不可见也不可派发。

#### 测试与验收

真实两个隔离执行环境，记录各自安装/文件系统身份；两台独立机器验证“跨电脑”，同机两个进程只能算协议测试。
覆盖重复领取、配对重放/过期、吊销、网络中断、Connector 重启、丢失 submit/回传响应、未知写入、恶意路径和越权 Artifact、迟到事件。
真实远程 Coding Agent 产物回传同一 Runtime，并能从平台查询；故障注入下没有第二次业务启动。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w10a"></a>

### W10A · 飞书人工任务通道

依赖：W04；对应阶段：M5；G-10 人工通道。

#### 结果与边界

把同一个 HumanRequest 投递为飞书人工待办，平台和飞书共享一次决定。与读写飞书文档的程序动作分开。没有测试应用、测试接收者及发送授权时不发真实消息，保持 live 验收 blocked。

#### 文件与步骤

新增建议 `channels/feishu.py`、`channels/outbox.py`、`api/channel_callbacks.py`、`tests/channels/test_feishu.py`、`tests/integration/test_feishu_human_live.py`。
1. 按所选飞书接口核对官方当前验签、加密、回调期限、重试、事件形状和权限，锁测试版本/应用 scope，不从旧笔记猜字段。
2. 冻结配置 Schema、外部消息↔HumanRequest↔真实主体映射；单次/重复投递走持久 outbox，失败能重试且不创建第二份人工事实。
3. 卡片只带所需引用/主题摘要/版本和短期访问方式；不携带服务长期管理员 token。飞书 user 身份经租户映射到 Runtime 主体。
4. 回调先验签/抗重放/持久接收再异步投递同一 decision 命令；预期版本/主题不符拒绝，机器人和转发者不自动有权。
5. 决定、过期、取消、重试后更新卡片；更新失败不回滚已提交决定，页面重新查询 Runtime 真相。
6. 独立 Console 可处理同一请求；飞书不可用不会把请求自动批准或丢失。

#### 测试与验收

定向 `tests/channels` 与 service/api 回归；覆盖错误签名、重复事件、旧卡片、超时、未授权用户、两个入口同时提交、发送成功响应丢失。
真实获准测试群/用户收到请求并完成一次决定，平台只推进一次；保留脱敏消息/request 对应证据，不公开真实账号。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w10b"></a>

### W10B · 飞书程序节点与副作用核对

依赖：W10A, W02；对应阶段：M5 外部程序执行。

#### 结果与边界

先实现一种窄动作：向指定测试表格追加一行，或所选官方接口确实支持且可按业务键查询的等效动作。具体验证选定后写死该包验收，不扩成完整飞书 SDK。

#### 文件与步骤

新增建议 `integrations/feishu_table.py`、配置/输入输出 Schema、`tests/integrations/test_feishu_table.py`、`tests/integration/test_feishu_program_live.py`。
1. 核对该接口当前权限和幂等/查询保证；若服务端没有原生幂等，用已授权可查业务键及持久映射做核对，不宣称普遍 exactly-once。
2. 在标准 Bridge 中注册窄程序能力，只允许绑定的测试资源/字段，不接受任意 URL/全租户管理请求。
3. 凭据在服务端解析，Agent 只接收结果；机器人应用权限与 W10A 真人审批权限独立。
4. 输入摘要、effectKey、业务键和远端记录 ID 持久关联；写入响应丢失先查询，无法证明时 unknown，不盲目追加。
5. 输出结构化结果与必要产物引用，支持程序失败/权限不足的准确诊断。

#### 测试与验收

模拟 401/403、429、重复提交、成功丢响应、查到/查不到但不确定、输入冲突；确认 retry 不造成重复业务。
获准测试资源真实执行及核对，只产生预期一条记录；测试清理受原权限约束且有记录。无法获得可靠核对时准确报告保证，不能标对应幂等能力已验证。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w11"></a>

### W11 · 一个真实宿主与独立 Console 共用接口

依赖：W04；对应阶段：M6；G-15 子集。

#### 结果与边界

证明任意技术栈通过 HTTP sidecar 最小接入是可行的：先提供一个参考宿主，再在用户授权的一个已有平台跑通。不能仅用参考页面冒充已有平台接入。

#### 文件与步骤

新增建议 `examples/host-integration/`、最小 TypeScript/Python 客户端（择宿主语言其一）、`tests/e2e/test_host_integration.py`；复用 Inspector/runtime client、RuntimeApplication/API。
1. 首先调查获准目标宿主，不改其他项目直到有明确接入范围；缺目标时完成参考宿主和测试并记录 live blocked。
2. 固定 host backend→Runtime 的身份委托，映射 namespace、subject 和授权资源。浏览器不能自行填写可信主体，服务不接受任意 X-User 作为认证。
3. 主体和角色校验进入共用 application 边界；现有单一服务主体配置不得冒充完整多租户 IAM。说明本轮限定的租户/身份模式。
4. 支持从业务对象启动 Run、查询/SSE、查看待办/产物、决定、暂停/恢复/取消；关联业务对象不修改宿主核心数据模型。
5. 可先嵌入最小面板/受控页面，保证凭据不入 URL、样式/路由隔离、退出释放订阅，状态仍由 Runtime 管理。
6. 安装第二种业务包，仅改配置/Binding，不再改宿主 Glue；独立 Console 同时可管理同一 Run。

#### 测试与验收

后端全门禁、前端测试/构建；测试 token 过期、伪造主体、跨 namespace、断线重连、幂等创建、窗口关闭、两端并发决定。
至少一个参考宿主和一个获准真实平台的证据；第二包业务需不同结构/Schema，不能只改标题。未获准真实平台接入时不擅自选择并修改其他仓库。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w12"></a>

### W12 · 干净目标交付与最小安装闭环

依赖：W09, W11；对应阶段：M6；G-13/G-14/G-16 声明子集。

#### 结果与边界

同一业务包在干净环境安装、绑定并运行，原开发服务关闭后仍可使用。在线可移植和离线运行分开声明；不把一次拷贝目录当作完整交付。

#### 文件与步骤

新增建议 `delivery/manifest.py`、`delivery/install.py`、`schemas/runnable-bundle.schema.json`、`tests/delivery/`、`tests/integration/test_clean_target.py`；扩展 CLI。
1. 冻结交付清单：包/业务 Schema、依赖与版本摘要、执行资产引用、要求的能力/权限/人工入口、配置模板和验证报告。
2. 导出不带 Secret、原生 session 目录、私人记忆/运行台账。导入只校验/暂存，绝不自动执行包中脚本。
3. 建立未激活的不可变 Deployment 快照；按目标补 Binding/授权后预检、授权试运行、再激活。测试通过的快照与激活快照必须相同。
4. 重复安装幂等；摘要不匹配/路径穿越/不支持版本拒绝；升级保留旧 Run 的固定定义；卸载不得删除仍被引用的产物/会话。
5. 干净容器/VM/目标用户环境，不共享源代码 checkout 或原开发机 session；安装锁定依赖并运行目标流程。
6. 离线样例若声明支持：预置必要模型/工具资源，禁外网执行，保存真实成功与无外联证据；没有条件则仅交付在线可移植预览，G-16 保持未完成，不宣传全离线。

#### 测试与验收

delivery 单元、目录遍历/压缩包攻击/缺依赖/幂等/版本迁移测试，完整 Python 门禁。
真实干净目标成功运行相同业务包，原开发服务断开不影响新 Run；业务包摘要保持。离线声明另有独立证据，不能因联网下载完依赖就称完全离线。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w13"></a>

### W13 · 作者效率、故障恢复与交付总验收

依赖：W04, W05B, W07, W08, W09, W10B, W11, W12；对应阶段：M7 可移植预览验收，不替代完整发布。

#### 结果与边界

确认之前的能力组合可用，而不只是各自的单元测试通过；形成版本固定的可移植预览交付报告。完整 V0.1 仍按第25章另审，不因本包通过自动发布。

#### 文件与步骤

新增 `evaluation/authoring/` 下合成/公开任务及结果 Schema（不可放私有评估数据）、`tests/e2e/test_portability.py`、`docs/release/PORTABILITY_PREVIEW.md`；维护报告索引。
1. 固定至少20个作者任务：创建6、局部修改5、错误修复5、目标重绑定4；输入/验收固定，包含缺能力、非法权限与旧版本诊断，不只测成功路径。
2. 作者可仅依赖短指南、CLI/目录/Schema 完成，不通过 GUI 配置隐藏字段。记录每任务首次验证成功、修复轮次、时间/Token、误授权、全部失败及成本；不编造历史对照数据。
3. 回归真实 Codex/Claude/Pi、一个程序、人类 Console/飞书、远程 Connector 的组合；同包换 Binding，业务包摘要保持。
4. 故障矩阵：丢提交响应、执行中断网、Worker/Bridge/Connector 重启、重复回调、权限撤销、session 切换失败、迟到观察。逐项说明恢复保证。
5. 验证第二业务/第二宿主无需修改 Runtime 核心；检查干净安装、升级/卸载、数据保留；用实际代码版本对应报告。
6. 发布说明按 implemented/verified/unsupported 列能力、OS/CLI兼容矩阵、许可和剩余 G 项；删除已失效示例和过度宣传。

#### 测试与验收

完整 Python/前端门禁及跨层真实验收；不得用“各里程碑以前通过”代替组合测试，但没有变化的昂贵实测可以引用匹配版本的既有证据并由 reviewer 确认覆盖。
零未经授权的真实批准/扩权，零 unknown 写入自动重发，所有待验能力明确标注；作者效率如实报告，不强求达到未测的10分钟目标。
独立 reviewer 审阅最终能力声明与证据矩阵，未验证能力不得列为已支持。本包结束交付预览，推送/发布仍依用户授权。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w15"></a>

### W15 · PostgreSQL 持久化、迁移与单活服务模式

依赖：W14, W02；对应阶段：M1/M2/M6；规范既定持久层。

#### 结果与边界

补齐规范要求的 PostgreSQL 17 服务 profile，并保留 SQLite 个人模式；采用 SQLAlchemy 2/Alembic 的规范边界。先一个调度 Worker，不引入分布式调度系统。所有迁移必须保住旧运行事实。

#### 文件与步骤

新增 `storage/`、`migrations/`、`tests/storage/`、`tests/integration/test_postgres.py`；逐步抽取 `runtime/ledger.py`，不能一次重写全部业务状态机。
1. 先抽最小 repository/transaction 边界，让现有 SQLite测试保持；用同一契约测试运行两个存储后端。
2. 迁移 runs/scopes/invocations/attempts、command receipts、waits、inbox/outbox、审批、事件、artifact、宿主/session关联；已有执行宿主私有库保持其所有权，不能直接跨库改状态。
3. PostgreSQL 用实际事务/唯一约束实现幂等、版本竞争、等待领取及原子决定+推进意图；测试隔离级别与并发失败重试，禁止仅靠 Python进程锁。
4. 实现单活调度租约/锁及 fencing；第二 Worker 必须拒绝派发，连接断开/锁丢失先停止新派发；旧执行仍按 unknown 核对。
5. Alembic前向迁移及备份恢复演练；禁止自动执行不可逆降级。SQLite→PostgreSQL使用停写、快照导出/导入、校验数量/摘要和唯一执行权交接，不在线双写。
6. 提供 dev/test Compose，锁镜像摘要；服务 liveness/readiness区分数据库就绪和真实执行可用。

#### 测试与验收

`tests/storage` 同构测试+真实 PostgreSQL集成；事务回滚、两客户端同时决定/提交、Worker双启动、数据库断线、写成功响应丢失、迁移中断和恢复必测。
真实从旧 SQLite fixture库升级/导入后继续一个等待 Run，旧产物/决定/事件摘要不变；同一个 Run不能在两套数据库同时调度。完整 Python门禁通过。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w16"></a>

### W16 · LangGraph 后端、一致性与完整组合语义

依赖：W15, W07；对应阶段：M1/M2/M4；全部控制流与 checkpoint。

#### 结果与边界

实现规范冻结的 LangGraph 后端，业务 Ledger仍是权威，checkpoint只记录控制进度。使用现有 graph/compiler 和运行台账，不把 provider对象写入公共协议。

#### 文件与步骤

新增 `backends/langgraph/`、`tests/backends/`、`tests/conformance/`；复用 runner/worker；锁持久 saver版本与依赖。
1. 对源码盘点 six primitives 的实际语义，先固定同构 conformance harness；检查当前 static parallel 是否只是顺序遍历，不能冒充满足并发/取消保证。
2. 编译为后端节点时，通过 Ledger读取已确认结果；外部提交必须先有 durable intent，节点重入不得重复副作用。
3. SQLite与PostgreSQL profile 使用对应持久 saver；明确 checkpoint失败、台账已提交、重入与恢复顺序。业务表和 saver无需假装跨系统原子事务，使用可恢复一致性边界。
4. 完成 workflow/parallel/repeat、失败/取消/超时传播、有界并发、汇合和本轮输入固定；并行一支失败先核对/取消其他活动支，不能留下幽灵执行。
5. 覆盖按源版本回放、包/Binding漂移、轮次/Attempt/内部步骤独立编号；旧预览 Run升级策略明确：受支持迁移或旧后端读/排空，不能静默重新执行。
6. 把内存 saver仅用于单元测试；生产 profile不允许无持久化启动。

#### 测试与验收

故障注入：结果落库后 checkpoint失败、checkpoint存在但观察未确认、人工等待重启、并行乱序/局部失败、repeat超限、子流程取消、旧Worker迟到。
同一 conformance suite在SQLite/PostgreSQL跑通；至少一次真实 mixed workflow跨重启继续且不重复模型任务/写入。不得仅比较最终文本而忽略调用次数、事件及审批。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w17"></a>

### W17 · 团队身份、授权与所有入口一致性

依赖：W14, W11, W09；对应阶段：M2/M6；可信主体与 namespace隔离。

#### 结果与边界

完成 team profile 的最小可信身份：一个组织、多用户/服务主体、隔离 namespace；不建设完整组织管理 SaaS。先实现一个真实 OIDC提供方及服务凭据机制；宿主委托走同一授权层。

#### 文件与步骤

新增 `auth/`、角色/策略Schema、`tests/auth/`、`tests/integration/test_identity_live.py`；修改API/RuntimeApplication/Connector/产物与会话访问边界。
1. 固定 owner/operator/author/reviewer/viewer/service 等最小权限矩阵或等价能力集；执行配置管理、审批、核对、凭据管理、安装激活分别授权。作者不能批准自己被要求真人的节点。
2. OIDC/JWKS或所选可信验证路径核对 issuer/audience/expiry/签名/轮换；浏览器身份不是任意 header/body actor。为自动化提供可撤销scope token，不以全权共享管理员 token替代。
3. 列表、查询、SSE、Artifact字节、原生session、命令和错误均按 namespace/主体过滤；保护对象存在性与跨租户引用。
4. 统一应用命令层授权，CLI、UI、飞书、宿主、worker恢复和reconcile不得绕过；背景执行使用冻结授权主题并检查撤销策略。
5. 登录/退出/会话过期/邀请或本地管理员bootstrap可用；初始秘密一次生成，不把dev-token发到发行包。
6. 明确 trusted-local单人模式与team模式差异；team不能无认证绑定公网接口。规范接口和配置同轮冻结。

#### 测试与验收

授权矩阵全表测试、错误issuer/audience/过期/撤销token、跨namespace读写/SSE/Artifact、伪造真人、任意actor、权限变更后继续派发。
真实两个用户加一个服务主体在参考IdP环境登录并验证不同权限；同一请求在CLI/UI/飞书/宿主结论一致。没有生产IdP账号可用本地真实OIDC测试部署，不能用fake header替代验收。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w18"></a>

### W18 · Secret Provider、受控提权与审计闭环

依赖：W17, W08, W07；对应阶段：M2/M3；G-05/G-18闭合。

#### 结果与边界

凭据不进入包/上下文/日志；程序和Agent通过受控工具代理使用最小权限；真正落实权限不足、批准、撤销、切换与恢复。

#### 文件与步骤

新增 `secrets/`、`policy/`、`tests/security/test_secret_lifecycle.py`、`test_privilege_requests.py`；扩展执行宿主/工具broker、审计与目录。
1. 至少实现一个本地受限Secret Provider和team可配置provider接口；长时secret存储与短时执行grant分开，记录解析者和最小资源scope。
2. Secret在trusted broker端解析；sandbox不获得不必要原始token。不得声称靠正则日志脱敏就实现秘密隔离；执行环境泄露范围必须可说明。
3. 权限请求绑定动作、资源、输入摘要、session/workspace/policy修订、TTL与次数；批准者与执行者分离，批准不覆盖外部服务实际拒绝。
4. 所有后端给出“requested / effective / enforced by / unsupported”，不能把某CLI权限选项硬等价于另一个CLI沙箱。
5. 撤销、轮换、目标变更后阻止新派发；已在途动作保留待核对，旧批准/旧会话历史不能给新目标授权。
6. 审计append-only应用接口+受限数据库角色，记录主体/原因/摘要；不承诺数据库管理员不能篡改。需要更强保证时导出受控不可变存储，明确实际边界。

#### 测试与验收

秘密在错误、stderr、SSE、包导出、Artifact预览、trace、core dump策略中的泄露负例；取消/轮换/重复权限回复/旧工作区批准复用均有测试。
真实受控任务：沙箱拒绝→窄能力请求→有权主体批准→仅目标动作成功；同批准不能执行第二资源/更宽命令。拒绝/超时不会自动切换trusted-local。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w19"></a>

### W19 · 完整 Agent 作者工具链：创建、修复、评测、Diff

依赖：W14, W13, W16；对应阶段：M4/M7；G-19闭合。

#### 结果与边界

Agent第一开发者从文档口号成为完整工具链；流程图只是视图。不做自动发布Agent，不允许通过改验收求绿。

#### 文件与步骤

新增 `authoring/`、`evaluation/`、`compiler/diff.py`、标准库/模板目录及锁文件、`tests/authoring/`、`tests/evaluation/`、`tests/conformance/test_standard_library.py`、CLI契约；维护短指南与机器上下文入口。
1. 提供可发现的resource/config Schema、最小脚手架、模板清单、版本化help与机器输出；从已安装资产发现，不要求下载全生态。
2. authoring命令只写指定新目录/草稿，保护已有文件；稳定ID、局部patch和明确基线摘要。并发修改返回冲突，不覆写他人工作。
3. eval固定输入/业务验收/环境和预算；分fixture/live，计入failed/unknown/cancelled及未知成本；输出回合、Token/成本、等待与调用事实。实际live须受授权资源和预算约束。
4. 语义diff覆盖图/Schema/权限/能力/依赖/session与工作区/验收变化；布局变动不产生业务diff。批准绑定确切变更摘要，不自动merge/deploy。
5. 实现校验/预检独立错误聚合、稳定错误码、expected/actual和建议；schema/examples/CLI文档通过同一测试同步。
6. Agent可仅用CLI/API完成固定任务集，不读取运行数据库、不用GUI隐藏配置。可选MCP作者工具只包装同一命令边界，非v1必装依赖。
7. 补齐规范§29标准组件描述与模板锁定：角色/协议无关的call、人工、验证、反馈、结构化汇总等组件可组合，至少内容交付与数据质量两个不同业务Preset，后者包含并行检查与有界修正；组件来源映射到原语/实例，不能仅改示例标题。W16提供底层控制流/Schema/并发恢复证据；本包实现模板展开、版本及传递依赖锁定，完成机器/人工gate、反馈、报告与product一致性、纯聚合的标准组件组合，最终签收CMP-01、CMP-05/06、CMP-12—26。

#### 测试与验收

至少20个固定任务沿用W13分类并跑新的可复现对照；对照不存在时不编造提升比例。公布首次通过率、修复回合、时间/Token/成本和失败全量。
要求全部“有效输入/明确拒绝”任务满足其预期，零因删约束造成的假成功；速度作为实测优化指标，不用随意分钟承诺替代准确性。


**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w20"></a>

### W20 · 可嵌入 SDK、完整人工入口与可访问 Console

依赖：W17, W19, W04；对应阶段：M4/M6；G-09/G-15闭合。

#### 结果与边界

独立Console与宿主嵌入共用SDK、身份、命令和状态；作者Agent仍主要用文件/CLI。只实现必要辅助编辑，不重造完整视觉编程产品。

#### 文件与步骤

拆分 `inspector/src/runtime` 中可复用client/view组件至版本化SDK包；新增组件测试、宿主fixture、浏览器e2e；保留旧入口迁移说明。
1. 从固定OpenAPI/事件Schema生成或验证类型；SDK提供启动、观察、决定、控制、Artifact和执行能力目录，错误与取消语义不另造。
2. 人工 review/input/file-upload 的默认表单支持矩阵明确；不支持Schema预检拒绝/要求已验证自定义入口，不静默丢字段。
3. 读侧显示定义/运行、scope/轮次/Attempt/内部步骤、未知状态与观察新鲜度；键盘/触屏可操作，产物预览转义，不运行不可信HTML。
4. 支持宿主样式隔离、卸载释放事件订阅、登录过期/重连/Last-Event-ID或快照恢复；全权token不进入URL或公共浏览器存储。
5. 辅助图编辑回写同一包模型，版本冲突可发现；布局持久化与业务语义分离，历史Run不可改写。
6. 在两个不同宿主集成（已有平台+不同技术栈参考宿主）使用同一SDK，安装第二业务包不改Glue；文档含完整样例和兼容声明。

#### 测试与验收

Python API/合约门禁+前端unit/build+真实浏览器e2e；重复操作、组件重复挂载/卸载、失效token、乱序事件、大图、键盘、错误Schema均覆盖。
满足规范基础UI性能目标并记录浏览器/设备；未实现字段、动态表单或原生轨迹不能在UI伪造为完整支持。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w21"></a>

### W21 · 运维、备份恢复、数据保留与故障手册

依赖：W15, W16, W18；对应阶段：M6/M7 服务可运维性。

#### 结果与边界

普通试用管理员可启动、诊断、备份、恢复、升级和安全清理；故障时不会多出一个调度权威。

#### 文件与步骤

新增 `ops/`、`docs/operations/`、`tests/operations/`、metrics/tracing边界；对接已有Worker/控制命令，不让遥测变成运行依赖。
1. liveness/readiness区分进程、数据库、迁移、活跃Worker和后端能力；提供队列滞留、unknown、审批期限、错误、积压、成本未知率指标。
2. 日志结构化且有Run/Attempt/dispatch/interaction关联；限额、采样与轮换不能丢控制事实。OTel/Langfuse关闭/不可用时核心仍运行。
3. 提供一致备份单元：业务DB、checkpoint、包/Binding快照、Artifact内容/摘要、必要Connector/session关联及秘密恢复依赖。使用停写或一致快照协议，不拼接任意时点文件。
4. 恢复到新实例时撤销/隔离旧调度权，先核对在途外部动作再启派发；恢复后不能以全新key重做未知写入。
5. 实现保留/删除策略、Artifact可达性GC、孤儿上传清理、审计保留、session/记忆删除传播；活动Run和审批证据不得被误删。
6. runbook覆盖磁盘满、DB不可达、CLI版本漂移、令牌过期、模型额度、Connector离线、升级失败；每项含命令、预期输出和升级处理路径。

#### 测试与验收

真实PostgreSQL/Artifact备份恢复、损坏/缺件拒绝、旧实例仍活跃、GC引用竞争、磁盘/日志压力、关闭遥测测试。
基准环境先设定并冻结RPO/RTO：计划备份恢复RPO=最近一次确认备份，目标RTO≤30分钟；未确认外部动作不受此时间强行推进。记录实际数据量/耗时及差距，不把定时备份等同零数据丢失。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w22"></a>

### W22 · 完全本地真实 Agent 与离线交付

依赖：W12, W18；对应阶段：M3/M6；G-16及离线强制验收。

#### 结果与边界

无需外部Agent平台也可完成真实Agent/程序/人协作；断外网的干净环境能安装和运行声明的离线组合。沿用Ollama基础，补齐真实工具/产物/限额而不是增加固定字符串演示。

#### 文件与步骤

扩展内建执行宿主和 `runtime/ollama.py`，新增离线Bundle profile、`tests/integration/test_offline_live.py`、模型/资源许可清单。
1. 选择一组可合法交付、硬件可运行且有结构化结果/工具能力的本地模型；固定权重/量化/运行时/Prompt版本，记录RAM/VRAM/CPU需求和质量样本。
2. 内建Agent执行授权材料读取、真实推理、窄工具使用、必要交互、事件、程序校验及Artifact交付；调用经过同一执行策略。
3. 离线包含或明确随附一切必需安装资产/依赖/模型资源；仅给下载链接不是离线包。许可不允许重分发的资源由受控本地导入，离线验收前完成并核摘要。
4. 关闭外网/DNS后全新部署，保留loopback/目标本地LAN所需访问；检查安装、许可检查、字体/前端、遥测和模型没有隐性外联依赖。
5. 独立人工界面可用；不能依赖飞书或远端账户完成离线人类任务。日志及诊断准确解释外网服务能力被禁用。
6. 使用两份不同职责、版本化的Agent配置（产出与评估）完成真实输入→双Agent模型/工具→程序→真人→产物闭环，签收RDI-06；两者不能由同一固定输出伪装。保存同环境重复运行结果，质量不足失败要算入报告。

#### 测试与验收

网络阻断真实执行+外联观测；重启恢复、产物摘要、模型缺失/不兼容/资源不足预检拒绝。真实模型输出不是预置fixture。
W12可先交付在线预览，但本包未通过不能将最终v1完整交付标为离线可用或全部验收通过。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w23"></a>

### W23 · 独立可安装的真实 Latent 实验实现

依赖：W14, W16, W18；对应阶段：E1/E2；LH-01—LH-14。

#### 结果与边界

满足现有规范第25.9的项目级实验交付门槛：默认关闭、单独安装的一组真实latent导出/消费后端。不是让Codex/Claude暴露隐藏状态，也不要求所有普通用户安装GPU依赖。

#### 文件与步骤

新增隔离 `experimental/latent/`、profile/codec契约、测试与评测脚本；公共包不import或探测该依赖。不得用“embedding当向量传递”冒充模型计算中的真实消费。
1. 先选具有可控源码和许可的开放模型/推理后端，验证两次独立逻辑调用间导出/导入真实表示的可行性，锁硬件、模型和Codec。若后端不支持，记录blocked，不能伪造成功。
2. 冻结route/profile、载荷格式/摘要/大小、源/目标/scope/轮次关联与实际消费回执；不安全反序列化、跨scope、多源融合和隐式KV共享拒绝。
3. export载荷进入受控Artifact存储，目标实际模型计算消费；审计摘要与latent输入分开，缺消费回执不能算成功，也不能静默文本回退。
4. 持久执行：响应丢失/重启复用同载荷与dispatch，unknown先核对；迁移/备份ACL与日志泄露测试齐备。
5. UI展示实际使用/未使用/失败；标准模式完全不安装latent依赖时所有普通功能正常。
6. 同包/输入/验收做开关对照，完整统计质量、Token/传输/计算/存储/失败成本；不要求实验优于文本，不筛掉失败或零消费样本。

#### 测试与验收

LH-01—LH-14逐项真实/fixture分层，至少LH-02和LH-14有真实模型与消费证据；损坏、伪载荷、版本漂移、缺回执、无权限、备份缺件均拒绝。
本包硬件/研究阻塞不阻止标准版其他开发和alpha试用，但按当前唯一规范会阻止“完整发布全部通过”。要移出门槛必须单独明确修改产品范围，后续Agent不得自行删此要求提速。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w24"></a>

### W24 · 发行包、供应链与升级回滚演练

依赖：W12, W15, W19, W20；对应阶段：M6/M7；G-13/G-14 完整发行形态。

#### 结果与边界

从固定源码构建可安装Python包、版本化前端/SDK、OCI镜像和Compose；依赖有锁、资产有摘要，管理员能验证、升级和回滚。预览的手工目录拷贝不作为最终安装方式。

#### 文件与步骤

新增 `packaging/` 或等价CI、Dockerfile/Compose、SBOM/许可证归集、`tests/packaging/`、`tests/integration/test_upgrade.py`。
1. 先冻结公开API/CLI/Schema兼容策略与弃用说明；选择继续稳定现有协议或新增版本，并提供明确转换器，不默默把multiverse/v0.1改成v1。
2. 构建固定依赖的wheel/sdist、SDK/前端静态资产、OCI镜像；编译阶段与运行权限分开，不用latest，不将开发数据库或凭据打进产物。
3. 生成SBOM、直接/传递依赖许可清单、构建来源与SHA256，配置签名/验证流程；未获得签名密钥时可开发但签名发行验收不能伪通过。
4. 全新personal/team部署可按文档启动；诊断端口/权限/目录/数据库缺口，默认安全配置，健康检查不执行付费模型任务。
5. 测试版本N→N+1：旧Run继续旧快照，新Run使用新部署；备份后迁移，失败后回滚应用/恢复DB的兼容矩阵明确。不可逆DB变更不能只降二进制。
6. 重复安装、卸载/排空、删除/保留、签名失效和同版本不同内容处理有明确命令。支持包导出与完整Bundle区别保持。

#### 测试与验收

实际构建产物安装验证，而非editable源码安装；校验所有摘要/签名；干净目标启动+升级中断+旧Run继续+回滚+卸载保留证据。
网络下载/签名不可用的步骤准确blocked；不得把未签名包标成已签名。最终发布产物须与被测试digest相同。
G-01安装ABI最终闭合：固定已审核执行资产/Adapter manifest、显式安装注册、版本共存、依赖校验、卸载活动引用约束与契约测试。安装不等于可用/verified，不实现自动发现任意Python模块或远程代码下载执行。


**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w25"></a>

### W25 · 支持矩阵、协议兼容与跨平台合约

依赖：W24, W05B, W09, W22；对应阶段：M7 兼容性。

#### 结果与边界

把“可移植”转为已实测支持矩阵；没有证据的系统/CLI版本不打支持标签。

#### 文件与步骤

扩展CI matrix、`tests/conformance/`、connector/SDK安装测试，维护W14矩阵及版本策略。
1. 对W14声明的每个OS/架构/Python/Node/浏览器/数据库组合运行对应最小必需测试；每一个 supported单元都有产物digest、安装与运行证据。
2. Codex/Pi/Claude固定安装版本至少分别跑新建/观察/取消/交互和声称的resume/fork；不支持的原生功能在能力协商里返回unsupported，不能影响其他基本能力。
3. CLI/HTTP/SDK/宿主四个入口对同一授权/幂等/版本冲突操作给出一致结果；协议旧客户端在支持范围内不崩溃，超范围明确拒绝。
4. 测路径分隔/Unicode/空格、换行、时间/时区、shell参数、大小写、进程树/信号与权限模型差异；不拼shell字符串执行用户输入。
5. 以标准能力合约测试任意新Adapter，目录的verified只由真实测试证据支持；生成给Agent可读的兼容诊断。
6. 不能提供某平台机器时保持相应单元blocked；调整支持范围必须明确修订W14/规范并重新审阅，不能删除失败行假装全绿。

#### 验收

完整矩阵与conformance全部支持单元通过；Linux服务/沙箱、Linux/macOS Connector均有真实环境证据。单机模拟OS环境不算另一平台。
Windows原生/多活等未承诺能力保持unsupported不算失败，但任何已承诺能力不得靠skip通过。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w26"></a>

### W26 · 安全、滥用边界与数据隔离总审计

依赖：W17, W18, W20, W24；对应阶段：M7 安全发布门槛。

#### 结果与边界

对实际支持部署做可复现安全验证与修复；测试以授权本地/测试环境为界，不扫描未知第三方系统。不把一次AI审阅当作安全认证。

#### 文件与步骤

新增 `docs/security/THREAT_MODEL.md`、`tests/security/`、依赖扫描配置、SECURITY.md；复用所有已有权限负例。
1. 列明信任边界：作者/包/模型输出/Artifact/原生CLI/Connector/宿主/通道回调/IdP/DB管理员；逐项描述攻击者能控制的输入和保护资产。
2. 覆盖归档路径穿越/符号链接/压缩炸弹、命令参数注入、任意文件读取、HTTP Job SSRF/重定向/本地元数据地址、Webhook重放、伪造主体、跨namespace对象引用。
3. 覆盖恶意文档/模型输出请求扩权、工具审批主题替换、旧批准复用、session越权、Secret泄漏、沙箱逃逸配置缺陷；权限应由代码/OS实施，Prompt不算防护。
4. 上传/下载大小、事件/日志压力、API速率、租约积压、费用限额有实际限制和拒绝证据；失败不能导致不受控重试。
5. 扫描锁定依赖/镜像，修复已确认影响支持部署的高危问题；不可修项需可验证缓解且经过独立审阅，不用“暂无人利用”豁免。
6. 写明漏洞反馈、分级、密钥轮换和应急停派发路径；独立reviewer另看最终修复，不由实施者自签。

#### 验收

零未解决P0/P1或等价高危/严重问题，零影响验收的P2；所有安全发现有复现、修复与回归。
记录测试覆盖和未覆盖的威胁；不宣称抵御恶意多租户/内核级攻击，除非另有真实隔离证据。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w27"></a>

### W27 · 性能、连续运行与灾难恢复压测

依赖：W16, W21, W25, W26；对应阶段：M7 稳定性门槛。

#### 结果与边界

在固定环境证明系统持续运行、恢复和资源约束；模型时延和人类等待不混作Runtime性能。禁止用丢控制事件/省授权提升数字。

#### 文件与步骤

新增 `benchmarks/runtime/`、故障注入脚本、`tests/operations/test_recovery_matrix.py`、性能/稳定性报告。
1. W14先冻结基准配置和门槛，默认team基准4vCPU/8GiB内存+SSD、独立受控执行端；记录DB/网络/模型位置与版本，模型费用另设预算。
2. 至少满足既有规范：100静态节点、20等待Run、10,000分页事件；首屏可交互≤2秒、已提交控制事件→本地UI可见≤1秒。补测100活动Run/20外部执行任务/200SSE订阅作为扩展目标，达不到不得声明该容量，基础门槛不能静默下调。
3. 6小时预检后进行连续72小时soak；混合提交/等待/决定/取消/重连/完成，主要用确定性executor控制成本，另有真实模型链抽样。用户可停止试验，但停止不算通过。
4. 注入Worker/Bridge/Connector崩溃、网络分区、DB断线/恢复、token撤销、磁盘压力和积压；保证零未经授权推进、零未知写入盲目重发、零已确认控制事实丢失。
5. 记录吞吐、P50/P95/P99、内存/FD/进程/磁盘、队列年龄和错误率；按固定节奏检查资源是否持续泄漏，明确冷启动/稳态区别。
6. 在负载中执行备份/恢复、滚动或停机升级与排空；验证W21 RPO/RTO目标和单活权威。测试作为可恢复后台任务，Agent中断后读取记录续跑，不能用压缩时间代替72小时。

#### 验收

完整72小时证据，基础SLO达标，零一致性/权限不变量破坏；synthetic非注入场景控制面成功率目标≥99.9%，计划故障单独统计不可隐藏。
严重故障修复后重跑受影响矩阵；若影响soak主要不变量/资源泄漏则重启完整72小时。失败仍保存全量汇总，不只报告最好一次。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w28"></a>

### W28 · 候选版真实试用与反馈闭环

依赖：W19, W20, W22, W24, W26；对应阶段：M7 Beta/RC 外部可用性。

#### 结果与边界

验证非原作者也能安装、创作、协作和恢复。可在W27长时测试期间开展，不能因为招募/真人不足伪造参与者。

#### 文件与步骤

新增试用手册、示例/问题模板、`docs/release/TRIAL_REPORT.md`、合成反馈样例；真实反馈/账号/私人业务数据只放受控位置。
1. 准备可校验RC产物、安装/升级/卸载说明、快速入门、20作者任务中的简化体验和反馈通道；说明试用数据/日志边界。
2. 默认验收样本：至少5套独立干净安装环境、2种宿主集成、10名真实试用者；全体累计≥500个已记录workflow runs，其中≥50次真实Agent任务且包含真实人工决定。合成跑数不能代替用户体验样本，记录所有失败和弃用。
3. 每个安装者只靠文档完成首次运行；记录安装阻塞、首个成功耗时、求助次数、作者修复回合，不把维护者暗中修环境后记为一键成功。
4. 覆盖个人独立、团队、远程电脑、飞书、离线的声明场景；试用者只接触获准测试资源，真实发送/写入按其授权进行。
5. 问题分级并回流负责工作包；阻塞/数据丢失/权限问题修复复测和子Agent复审，影响安装包的修复生成新RC并重复受影响真实试用。
6. 给出兼容已知问题、支持边界、诊断收集方法和升级建议。没有足够真人/环境就保持blocked，不启动伪用户脚本凑人数。

#### 验收

上述覆盖/样本达到，所有P0/P1和影响核心使用的P2关闭，试用者可在文档引导下完成核心场景；剩余P3明确列出。
本包完成不自动代表稳定发布，还需W23现行规范门槛、W27soak和W29最终固定产物验收。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


<a id="w29"></a>

### W29 · v1.0 稳定试用版最终签收与发布准备

依赖：W14, W15, W16, W17, W18, W19, W20, W21, W22, W23, W24, W25, W26, W27, W28；对应阶段：M7 完整项目级门槛。

#### 结果与边界

输出固定版本v1.0.0的稳定可大范围试用交付物和完整证据。软件稳定版仍明确可选/实验能力，不能把latent实验标为通用生产保证。推送dev已获授权，创建Release/发布包注册表/生产部署不由此自动授权。

#### 文件与步骤

维护版本/CHANGELOG、README、SUPPORT_MATRIX、ACCEPTANCE.json、发行清单/签名和稳定版指南；报告关联最终源码/依赖/产物digest。
1. 对附录要求逐ID核查，所有现行必需AC/LH/SUP/RDI/CMP/PORT和G冻结项有证据；任一failed/unverified/blocked不得写“全部通过”。协议无需为了软件1.0自动改名。
2. 冻结候选源码与依赖，实际构建发行物；验收之后任何源码/Schema/依赖/镜像改变都形成新候选并重跑受影响测试，不复用旧digest的结论。
3. 跑完整Python/前端/conformance/安装升级/权限及组合测试；昂贵live/soak可引用匹配版本的有效证据，由reviewer确认变更影响，不能默认豁免。
4. 独立子Agent审阅最终代码变化、能力声明、全部报告和发布清单；必要时分别审安全/恢复，但至少一名reviewer负责汇总判定。
5. 无高危/阻塞缺陷、所有必需真实验收及72小时稳定性/试用样本满足；已知限制/实验能力/不支持平台清晰。形成release-ready状态。
6. 按共同规则提交推送dev并核对远端；准备发布说明、精确tag/包清单及校验摘要。用户授权公开发行后才创建正式tag/GitHub Release/包发布；否则保持可下载本地产物+已推源码的release-ready，不说已经对外发布。
7. 提供30天试用维护流程：反馈分级、错误诊断、补丁分支、回归/独立审阅/推送与新签名产物；维护承诺由实际维护安排确认，不伪造持续监控。

#### 完成标准

v1.0.0产物可核验、可安装、可升级回滚、可观测恢复，Agent作者路径和人机协作已实测；范围内所有规范验收有证据，最后子Agent审阅approved，代码及报告推送远端可核对。
公开发行操作另记 release_status=release_ready / released，不能用“推送dev”冒充已发正式版本，也不能为了发布把未满足项改成不适用。

**本包收尾：**执行§3全部适用门禁，真实调用独立子Agent审阅，修复复测复审，提交并推送origin/dev，验证实现与状态回执均到远端，再进入依赖包。


## 6. v1.0 发布门槛总表

以下是规划目标而非当前测试结论。W14将新增要求写入唯一规范；W29逐项签收，任何mandatory项缺证据不能发布成全部通过。

| 门槛 | 最低要求 | 主负责 |
|---|---|---|
| 真实核心闭环 | Agent/程序/真人/产物，关闭页面和重启不丢事实 | W03/W04/W16 |
| Agent作者体验 | 创建/修改/修复/重绑定20任务；完整Schema/诊断/eval/diff | W13/W19 |
| 可替换性 | Codex、Claude、Pi真实执行；同包换Binding；明确原生能力差异 | W05A/W05B/W25 |
| 会话与记忆边界 | 多会话单写、resume/fork能力协商、跨CLI授权续接、权限收窄清理 | W06/W07/W18 |
| 远程/飞书/宿主 | 第二机器、真实回调、真实程序写入、两个宿主技术栈 | W09/W10A/W10B/W11/W20 |
| 持久服务 | PostgreSQL真实事务、LangGraph持久checkpoint、单活/恢复 | W15/W16 |
| 隔离与安全 | 实际沙箱、可信身份、Secret生命周期、跨namespace拒绝、无阻塞安全缺陷 | W08/W17/W18/W26 |
| 安装与运维 | 固定发行物、干净安装、旧Run升级兼容、回滚、备份恢复/GC | W12/W21/W24 |
| 离线 | 禁外网真实本地模型/工具/真人/产物，资源依赖闭合 | W22 |
| 实验交付 | 独立安装真实latent导出/消费，LH全验收；默认不加载 | W23 |
| 兼容性 | W14声明的所有supported矩阵单元有真实证据，无skip冒充 | W25 |
| 稳定性 | 72小时soak、基础UI/事件SLO、单活和故障矩阵、无一致性破坏 | W27 |
| 试用 | 5独立安装、2宿主、10真人、500 Run含50真实Agent；所有失败计入 | W28 |
| 证据完整性 | 每一必需验收ID有owner、测试/真实证据和结论；最终产物digest一致 | W14/W29 |
| 提交交付 | 最终review approved，实现/报告/状态回执均在origin/dev | 每个包 |

“大范围试用”不是无限容量或敌对多租户生产认证。容量、模型质量和平台支持只按实测数据声明。维护成本高或缺硬件的必需功能可以阻塞最终发布，不能用换一个名称跳过已有约定。

## 7. 给子 Agent 的审阅提示（可直接复制）

```text
你是独立只读reviewer，不是实现者。审阅 /home/aied/tanyicheng/multiverse 的 <ID>。
阅读工作包规划.md 对应工作包及唯一规范相关章节。
基线：<HEAD及开始时已有改动清单>。
最终范围：<包含未跟踪文件的清单及SHA256记录>。
报告/测试/真实验收：<路径或受控证据引用>。
直接检查实际diff、新文件和测试；不要只读实现者摘要。
检查契约、权限/身份、副作用幂等、崩溃/unknown、取消/迟到结果、
session/工作区隔离、兼容迁移、产物和Agent可修复诊断。
不编辑文件、不读取私人凭据、不触发外部副作用。
输出 verdict=approved/changes_requested/blocked；reviewed_snapshot；
实际审阅范围；逐项 findings(ID/级别/文件:行/触发/影响/建议/是否阻塞)；
验收项passed/failed/unverified及证据；剩余限制。
未发现问题也要说明范围。缺真实验收不可仅凭unit tests批准。
```

复审时提供新摘要、每条发现对应修复及复测；实际工具task ID/返回记录必须保存。不允许由主Agent复制此提示后自己输出“审阅通过”。

## 8. 里程碑交接模板

```text
ID / status / implementation_ready:
执行Agent、日期、基线HEAD、开始时已有修改:
目标、实际修改文件和最终快照SHA256:
规范/G项/Schema变更与兼容说明:
验收矩阵: 验收项 | 实现位置 | fixture/真实证据 | 结果
测试: cwd | 命令 | 版本 | 时间 | 退出码 | passed/failed/skip | 日志索引
独立reviewer task ID / reviewed_snapshot / verdict / 原始返回索引:
发现: ID/级别 | 修复 | 复测 | reviewer复审 | 剩余非阻塞项owner
真实条件/未支持/blocked原因:
实现提交C、远端包含C的证据:
状态回执R、远端包含R的证据:
下一个依赖已满足且已推送的工作包:
```

不要把令牌、真实对话、客户业务数据和private evaluation写进报告。公开报告只保留最小脱敏证据与摘要；无法公开的原始证据须由有权reviewer实际检查。

## 9. 本篇规划的完成与后续执行

本篇规划本身以结构/依赖/链接/验收覆盖校验及独立子Agent审阅作为门禁，不需要运行尚未实现的32个工作包。当前已有代码随本次推送时单独测试和review，不由文档review替代。

下一位Agent从W00开始；不要把本篇被提交推送当作W00—W29已完成。开发中只维护本篇范围与STATE进度，不恢复旧分包为另一套路线。



## 附录A：现行规范逐项验收负责人

此表主负责表示该验收ID的完整证据签收工作包，不代表今天通过；详细必须结果仍读取唯一规范相应ID。早期包只贡献分阶段证据，不因尚未具备后续能力而提前签收完整ID。W14核验每条签收owner的依赖闭包确实覆盖所需实现；若缺依赖则同轮补齐STATE与本篇，不允许暗藏验收依赖环。W29最终检查，所有项目初始待验。

| ID | 规范场景/能力 | 主负责工作包 |
|---|---|---|
| G-01 | 注册式 Adapter，按平台复用。 | [W24](#w24) |
| G-02 | 执行管理归属与实际位置分开。 | [W01](#w01) |
| G-03 | AgentProfile / 程序配置版本化。 | [W06](#w06) |
| G-04 | Managed Execution Host 执行与恢复。 | [W03](#w03) |
| G-05 | 隔离与资源限制要实际实施。 | [W08](#w08) |
| G-06 | Connector 配对与可撤销身份。 | [W09](#w09) |
| G-07 | 出站接入、持久领取及断线恢复。 | [W09](#w09) |
| G-08 | Agent/工具内部步骤可观察。 | [W03](#w03) |
| G-09 | 人工表单与外部查看入口。 | [W20](#w20) |
| G-10 | 飞书等渠道与 Runtime 关联。 | [W10A](#w10a) |
| G-11 | 运行中可选交互，不改原始输入。 | [W03](#w03) |
| G-12 | Artifact 上传/外部登记完整闭环。 | [W09](#w09) |
| G-13 | Runnable Bundle 与依赖闭合检查。 | [W24](#w24) |
| G-14 | 安装/激活/更新/卸载可验证。 | [W24](#w24) |
| G-15 | 宿主一次接入、多流程复用。 | [W20](#w20) |
| G-16 | 完全本地真实 Agent。 | [W22](#w22) |
| G-17 | 多会话身份、原生绑定与安全切换。 | [W07](#w07) |
| G-18 | 权限变化与执行策略映射。 | [W18](#w18) |
| G-19 | Agent 优先编辑与语义变更审阅。 | [W19](#w19) |
| AC-01 | 导入合法包 | [W01](#w01) |
| AC-02 | 重复键、未知必需功能、非法引用 | [W01](#w01) |
| AC-03 | 显式同级图环、递归或超限组合 | [W01](#w01) |
| AC-04 | 未绑定 slot、错误能力或不支持 Skill | [W01](#w01) |
| AC-05 | Binding 权限超过部署/用户授权 | [W17](#w17) |
| AC-06 | 相同包切换两种执行实现 | [W13](#w13) |
| AC-07 | 同一命令重复 POST | [W02](#w02) |
| AC-08 | 同 key 不同请求正文 | [W02](#w02) |
| AC-09 | 外部创建成功后响应丢失 | [W02](#w02) |
| AC-10 | 保存提交意图后进程崩溃 | [W02](#w02) |
| AC-11 | 业务结果落库后 Checkpoint 保存失败 | [W16](#w16) |
| AC-12 | 等待人工时重启 Runtime | [W04](#w04) |
| AC-13 | 人工双击/重复提交 | [W04](#w04) |
| AC-14 | 审批对象版本改变 | [W04](#w04) |
| AC-15 | 人工到期/拒绝 | [W04](#w04) |
| AC-16 | 外部终态消息重复、乱序 | [W02](#w02) |
| AC-17 | 用户取消与结果完成竞争 | [W02](#w02) |
| AC-18 | 无法确认外部已停止 | [W02](#w02) |
| AC-19 | 暂停时已有工作完成 | [W02](#w02) |
| AC-20 | 写入后超时 | [W02](#w02) |
| AC-21 | 安全重试 | [W02](#w02) |
| AC-22 | executor 拥有内部重试 | [W02](#w02) |
| AC-23 | switch 多项同时为 true | [W16](#w16) |
| AC-24 | parallel 乱序完成 | [W16](#w16) |
| AC-25 | parallel 部分失败 | [W16](#w16) |
| AC-26 | repeat 达上限 | [W16](#w16) |
| AC-27 | 子流程失败/取消 | [W16](#w16) |
| AC-28 | 输出 Schema 错误或产物缺失 | [W09](#w09) |
| AC-29 | Artifact 被改动或跨 namespace 访问 | [W09](#w09) |
| AC-30 | 切换包或 Binding 新版本 | [W24](#w24) |
| AC-31 | 回滚部署 | [W24](#w24) |
| AC-32 | 无 Langfuse/无 OTel 后端 | [W21](#w21) |
| AC-33 | 外部执行器没有内部 Trace | [W20](#w20) |
| AC-34 | SSE 断线/游标过期 | [W20](#w20) |
| AC-35 | 嵌入 UI | [W20](#w20) |
| AC-36 | CLI 与 UI 执行同一操作 | [W17](#w17) |
| AC-37 | Eval 含失败、未知和无成本样本 | [W19](#w19) |
| AC-38 | 恶意归档、任意安装脚本、恶意 HTML | [W24](#w24) |
| AC-39 | Worker 双启动 | [W15](#w15) |
| AC-40 | 数据库+Artifact+Checkpoint 备份恢复 | [W21](#w21) |
| AC-41 | 真实非 Mock 外部执行器 | [W13](#w13) |
| AC-42 | 第二个非同类业务示例 | [W19](#w19) |
| AC-43 | 未知状态的人工核对 | [W17](#w17) |
| AC-44 | 凭据/权限撤销、执行器版本漂移 | [W17](#w17) |
| AC-45 | 日志超限、遥测中断 | [W21](#w21) |
| AC-46 | 关闭浏览器/CLI 连接 | [W20](#w20) |
| AC-47 | 通信字段缺省或 enabled=false | [W23](#w23) |
| AC-48 | 不安装 latent 插件、推理库、模型或向量服务 | [W23](#w23) |
| AC-49 | enabled=true 但 routes 缺失、实现不可用或不兼容 | [W23](#w23) |
| LH-01 | 同一包同一组兼容执行器切换 enabled | [W23](#w23) |
| LH-02 | 两个逻辑调用间真实交接 | [W23](#w23) |
| LH-03 | 假向量、转成字符串、仅注入审计摘要 | [W23](#w23) |
| LH-04 | 模型/profile/Codec 版本不一致或漂移 | [W23](#w23) |
| LH-05 | 开启但路由不满足同 scope/依赖/单源限制 | [W23](#w23) |
| LH-06 | 载荷损坏、过大或使用危险序列化 | [W23](#w23) |
| LH-07 | 导出后重启、接收端提交响应丢失 | [W23](#w23) |
| LH-08 | 实际消费回执缺失、摘要不匹配或 consumed=false | [W23](#w23) |
| LH-09 | 活动 Run 期间关掉/打开开关 | [W23](#w23) |
| LH-10 | 人工/程序混合节点、分支未经过 route | [W23](#w23) |
| LH-11 | 重复、并行和有界循环中的交接 | [W23](#w23) |
| LH-12 | 审计摘要、证据和审批关联 | [W23](#w23) |
| LH-13 | 跨 namespace/更低权限读取、载荷备份恢复 | [W23](#w23) |
| LH-14 | 开关对照与真实成本报告 | [W23](#w23) |
| SUP-01 | 外部 Agent 从自然语言创建新流程 | [W19](#w19) |
| SUP-02 | 作者读取不支持功能或不存在执行器 | [W19](#w19) |
| SUP-03 | 语法、引用或业务字段错误 | [W19](#w19) |
| SUP-04 | 为获得通过而降低验收 | [W19](#w19) |
| SUP-05 | 机器可读命令输出 | [W19](#w19) |
| SUP-06 | 静态、Mock、真实执行混合报告 | [W19](#w19) |
| SUP-07 | 人、程序、Agent 执行同一可替换契约 | [W13](#w13) |
| SUP-08 | 要求真人的审批被 Agent 模拟 | [W17](#w17) |
| SUP-09 | 程序成功返回 valid=false / 人拒绝 | [W16](#w16) |
| SUP-10 | 退出 0 / HTTP 200 但结果不合规 | [W16](#w16) |
| SUP-11 | 人工页面展示与提交 | [W20](#w20) |
| SUP-12 | 表单不支持某种契约 | [W20](#w20) |
| SUP-13 | 默认值、页面打开或通知已读 | [W20](#w20) |
| SUP-14 | input 型人工任务 | [W20](#w20) |
| SUP-15 | 人工文件上传并交付 | [W20](#w20) |
| SUP-16 | 部分上传、任意路径或非法 ArtifactRef | [W20](#w20) |
| SUP-17 | 上传与提交的跨权限引用 | [W20](#w20) |
| SUP-18 | 人工等待时关闭浏览器或重启 | [W20](#w20) |
| SUP-19 | 并发/重复/过期/取消后提交 | [W20](#w20) |
| SUP-20 | 内建页与嵌入页处理同一请求 | [W20](#w20) |
| SUP-21 | 外部人工服务回传或通知丢失 | [W10A](#w10a) |
| SUP-22 | 人声明外部工作已完成 | [W04](#w04) |
| SUP-23 | 页面与结构化 CLI 提交同一类结果 | [W19](#w19) |
| SUP-24 | Agent 生成程序源码 | [W19](#w19) |
| SUP-25 | 遗留程序副作用结果未知 | [W19](#w19) |
| SUP-26 | 切换标准/latent | [W23](#w23) |
| SUP-27 | latent 结果交给程序或人 | [W23](#w23) |
| SUP-28 | 全部能力的对外支持声明 | [W29](#w29) |
| RDI-01 | 统一调用契约 | [W01](#w01) |
| RDI-02 | 内外绑定替换 | [W01](#w01) |
| RDI-03 | 管理归属与位置 | [W01](#w01) |
| RDI-04 | 注册式扩展 | [W01](#w01) |
| RDI-05 | 能力支持报告 | [W01](#w01) |
| RDI-06 | 全内建真实协作 | [W22](#w22) |
| RDI-07 | 内建程序包装 | [W03](#w03) |
| RDI-08 | 内建 Agent 内部步骤 | [W03](#w03) |
| RDI-09 | 业务失败与执行失败 | [W03](#w03) |
| RDI-10 | 配置版本固定 | [W03](#w03) |
| RDI-11 | Session 隔离 | [W06](#w06) |
| RDI-12 | 工作区隔离与并发 | [W07](#w07) |
| RDI-13 | 资源与凭据 | [W18](#w18) |
| RDI-14 | 导入不执行源码 | [W24](#w24) |
| RDI-15 | 真实隔离声明 | [W08](#w08) |
| RDI-16 | 输出不合规 | [W02](#w02) |
| RDI-17 | 同 key 不同语义 | [W02](#w02) |
| RDI-18 | 决定后进程退出 | [W02](#w02) |
| RDI-19 | 提交回执丢失 | [W02](#w02) |
| RDI-20 | 运行能力门禁 | [W01](#w01) |
| RDI-21 | 远程配对 | [W09](#w09) |
| RDI-22 | 出站接入 | [W09](#w09) |
| RDI-23 | 一端多执行配置 | [W09](#w09) |
| RDI-24 | 重复领取 | [W09](#w09) |
| RDI-25 | 断线状态 | [W09](#w09) |
| RDI-26 | 重新连接 | [W09](#w09) |
| RDI-27 | 无法核实旧进程 | [W09](#w09) |
| RDI-28 | 外部平台对象选择 | [W09](#w09) |
| RDI-29 | 远端保证不足 | [W09](#w09) |
| RDI-30 | 重试责任唯一 | [W09](#w09) |
| RDI-31 | 人工结构化结果 | [W20](#w20) |
| RDI-32 | 人工入口可用性 | [W20](#w20) |
| RDI-33 | Web 与飞书同时操作 | [W10A](#w10a) |
| RDI-34 | 可信操作者 | [W17](#w17) |
| RDI-35 | 多流程同一会话 | [W10A](#w10a) |
| RDI-36 | 外部文件提交 | [W20](#w20) |
| RDI-37 | 内部多轮交互 | [W03](#w03) |
| RDI-38 | 开始和暂停派发 | [W16](#w16) |
| RDI-39 | 停止与完成竞争 | [W16](#w16) |
| RDI-40 | 停止不影响共享资源 | [W16](#w16) |
| RDI-41 | 重跑与单节点调试 | [W16](#w16) |
| RDI-42 | 双击与替代入口 | [W20](#w20) |
| RDI-43 | 定义与实例区分 | [W20](#w20) |
| RDI-44 | 循环三层区分 | [W20](#w20) |
| RDI-45 | 外部观测范围 | [W20](#w20) |
| RDI-46 | 重新订阅与关闭页面 | [W20](#w20) |
| RDI-47 | 外链与嵌入安全 | [W20](#w20) |
| RDI-48 | 完全离线真实运行 | [W22](#w22) |
| RDI-49 | Latent 非默认依赖 | [W23](#w23) |
| RDI-50 | Latent 交付与切换 | [W23](#w23) |
| RDI-51 | 交付依赖完整性 | [W24](#w24) |
| RDI-52 | 无隐含源环境依赖 | [W24](#w24) |
| RDI-53 | 干净环境安装 | [W24](#w24) |
| RDI-54 | 原开发平台不可达 | [W24](#w24) |
| RDI-55 | 宿主身份与命令复用 | [W11](#w11) |
| RDI-56 | 异步业务调用 | [W11](#w11) |
| RDI-57 | 流程包与完整 Bundle | [W24](#w24) |
| RDI-58 | 兼容性与离线资源 | [W24](#w24) |
| RDI-59 | 重复安装及启动 | [W24](#w24) |
| RDI-60 | 升级与回滚 | [W24](#w24) |
| RDI-61 | 卸载与共享运行环境 | [W24](#w24) |
| RDI-62 | 目标环境备份恢复 | [W21](#w21) |
| RDI-63 | 原生作者闭环 | [W19](#w19) |
| RDI-64 | 真实证据与完成声明 | [W29](#w29) |
| CMP-01 | 同模板从 2 Agent 扩展为 3 Agent＋程序＋人 | [W19](#w19) |
| CMP-02 | 纯程序或纯人工流程 | [W16](#w16) |
| CMP-03 | 角色改名、复用执行配置 | [W16](#w16) |
| CMP-04 | 仅用两条分支表达并行 | [W16](#w16) |
| CMP-05 | 模板展开 | [W19](#w19) |
| CMP-06 | 模板版本和传递依赖 | [W19](#w19) |
| CMP-07 | 未安装组件/执行器 | [W16](#w16) |
| CMP-08 | Schema 字段缺失、null 与类型不匹配 | [W16](#w16) |
| CMP-09 | 相同结构但能力/权限不同 | [W16](#w16) |
| CMP-10 | 目标字段静态无法证明兼容 | [W16](#w16) |
| CMP-11 | 数据引用指向未选中分支 | [W16](#w16) |
| CMP-12 | 开发交付新版本 | [W19](#w19) |
| CMP-13 | 评估报告缺失、重复、未知 ID | [W19](#w19) |
| CMP-14 | report subjects 与 product 不一致 | [W19](#w19) |
| CMP-15 | findings 有阻断项但 verdict=pass | [W19](#w19) |
| CMP-16 | 分支完成顺序颠倒 | [W19](#w19) |
| CMP-17 | 并行一个分支失败/停止未知 | [W19](#w19) |
| CMP-18 | 机器要求修改 | [W19](#w19) |
| CMP-19 | 人要求修改与拒绝 | [W19](#w19) |
| CMP-20 | 人不参与的一轮 | [W19](#w19) |
| CMP-21 | always 人工策略且机器未通过 | [W19](#w19) |
| CMP-22 | 多个独立真人请求 | [W19](#w19) |
| CMP-23 | repeat 达到上限仍需修改 | [W19](#w19) |
| CMP-24 | 轮次输出与下一轮输入 | [W19](#w19) |
| CMP-25 | 业务反馈缺少明确内容 | [W19](#w19) |
| CMP-26 | 网络重发或恢复 | [W19](#w19) |
| CMP-27 | Context 超限 | [W07](#w07) |
| CMP-28 | 跨机器相同路径 | [W07](#w07) |
| CMP-29 | Artifact 无权限、内容变更 | [W07](#w07) |
| CMP-30 | 外部自带返工/重试 | [W09](#w09) |
| CMP-31 | 外部完整工作流作为 call | [W09](#w09) |
| CMP-32 | 运行、草稿编辑、演示通道 | [W20](#w20) |
| CMP-33 | Scope 全失败或全未运行 | [W20](#w20) |
| CMP-34 | latent 路由跨轮次或多源隐式融合 | [W23](#w23) |
| CMP-35 | 标准模式相同组合 | [W23](#w23) |
| CMP-36 | 输出代码或任意 URL | [W24](#w24) |
| CMP-37 | 第二种非开发业务 | [W19](#w19) |
| CMP-38 | 干净目标环境安装组件模板 | [W24](#w24) |
| CMP-39 | 作者完成第二套流程 | [W19](#w19) |
| CMP-40 | Fixture、真人、模型、恢复报告 | [W29](#w29) |
| PORT-01 | Agent 独立完成文件编写与修改 | [W19](#w19) |
| PORT-02 | 第二流程与第二目标 | [W11](#w11) |
| PORT-03 | 本地注册预检 | [W01](#w01) |
| PORT-04 | 原生恢复与跨 CLI 续接 | [W07](#w07) |
| PORT-05 | 并发与失联切换 | [W07](#w07) |
| PORT-06 | 工作区及权限变化 | [W07](#w07) |
| PORT-07 | 飞书两种接入 | [W10B](#w10b) |
| PORT-08 | 作者效率与辅助编辑 | [W20](#w20) |
