# Multiverse 开发 Agent 入口

本项目位于 `/home/aied/tanyicheng/multiverse`，开发分支为 `dev`。遵守上级工作区 AGENTS.md；先核实 pwd、realpath、git status，保留已有改动。

- 先读根目录 [工作包规划.md](工作包规划.md) 与 [STATE.json](docs/development/STATE.json)。本篇是唯一施工计划；产品行为规范为 `docs/spec/MULTIVERSE_SPEC.md`，实际支持性以代码和测试证据为准。
- Agent 是第一开发者：文件/Schema/CLI/API 优先，GUI 辅助；复用既有 Runtime 和 HTTP Job，避免第二套事实权威。
- 每个实现里程碑必须：实现 → 必需测试 → **实际调用独立子 Agent 审阅** → 修复/复测 → 子 Agent 复审最终快照 → 提交并推送 → 核对远端 → 状态回执推送。主 Agent 自评不能替代审阅。
- 测试与审阅绑定具体文件快照，包含未跟踪文件；缺真实集成/必需测试/独立审阅不能标完成。细则、提示与报告模板统一见《工作包规划.md》§3、§7、§8。
- 用户已明确授权：本次处理完成后及以后每个里程碑完成后，都正常提交并推送 `origin/dev`；不得重复请求许可。禁止 force push；生产部署、正式 Release 和包注册表发布另按用户授权。
- 实现提交与状态回执均验证在远端后才能进入依赖包。推送失败保存待推送状态，下一轮优先续推；不能将本地 completed 当远端完成。
- 默认每轮一个工作包；用户授权连续执行时仍逐包过门禁。缺外部条件记录 blocked，可继续其他就绪包；不扫描私人会话/凭据或发送未授权消息。
- 此规则明确授权并要求独立里程碑审阅子 Agent，不要求为实现额外组建并行团队。规划任务不自动授权提前执行全部工作包。
